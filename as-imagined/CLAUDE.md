# Emerald Battle Engine Clone — Project Guide for Claude Code

## What this project is

A standalone recreation of Pokémon Emerald's turn-based battle system in
Godot 4.x, using GDScript, based on the **expanded/upgraded battle engine**
(DizzyEgg's battle engine upgrade, as found in `pokeemerald_expansion`) rather
than vanilla Gen III mechanics. This means modern conveniences and later-gen
mechanics are in scope and expected — Physical/Special split, newer abilities,
newer moves, etc. — not just what shipped in the original 2004 release.

This is a separate project from my other Godot project (a farming/action-RPG
hybrid). Do not assume any code, autoloads, or conventions from that project
carry over here unless I explicitly say so.

## Ground truth / reference

The authoritative source for "what should this do" is the
`pokeemerald_expansion` repository (DizzyEgg / ROM Hacking Hideout community),
NOT the Bulbapedia prose description and NOT vanilla `pret/pokeemerald` —
vanilla Emerald is missing the mechanics this project wants (Physical/Special
split, newer movepool, newer abilities), so it's not the right reference here.

- Primary reference repo: `pokeemerald_expansion` (battle engine upgrade
  branch / merged `master`)
- Located at: `reference/pokeemerald_expansion` (relative to project root)
  (clone this in if not present — see Setup below)
- Key files to consult when implementing a mechanic:
  - `src/battle_script_commands.c` — move/effect execution logic
  - `src/battle_util.c` — damage calc helpers, status application
  - `src/battle_ai_script_commands.c` and `src/battle_ai_main.c` — trainer AI
  - `src/data/moves_info.h` / similar — move data tables
  - `src/data/abilities.h` — ability definitions
  - `include/constants/battle_move_effects.h` — move effect IDs

When implementing any mechanic, the workflow is: **look at the source logic
first, then port the behavior to GDScript** — don't guess from memory of "how
Pokémon battles work," since even the expanded engine has specific numeric
constants and edge cases worth confirming against source rather than assuming.

If a mechanic's source location is unclear, search the repo structure before
implementing, or ask me to confirm before making something up.

## Tech stack

- **Engine:** Godot 4.x, Standard edition (GDScript, not C#/.NET)
- **Why GDScript:** consistency with my other Godot project, plus standard
  edition keeps Web export available if I ever want it
- **Data format:** Godot `Resource` (`.tres`) or plain `.json` for
  Pokémon/move/ability/item data — decide and lock this in during Milestone 1,
  don't mix formats later
- **No external plugins/addons** unless I approve one explicitly

## Architecture overview

```
scripts/
  data/           # Data resource classes (PokemonSpecies, MoveData, AbilityData, ItemData)
  battle/
    core/         # BattleState machine, BattlePokemon (runtime instance), turn order/priority resolver, damage calculator
    moves/        # Move effect implementations, tiered by complexity (see Build Order)
    abilities/    # Ability hook implementations
    ai/           # Trainer AI decision logic
  ui/             # Battle HUD, menus, text box controller
scenes/
  battle/         # Battle scene(s), UI scene(s)
data/
  pokemon/        # Per-species data files
  moves/          # Per-move data files
  abilities/      # Per-ability data files
  items/          # Per-item data files
reference/
  pokeemerald_expansion/  # Cloned source, read-only reference — never edit
docs/             # Design notes, decisions log, mechanic verification notes
```

### Core battle flow (state machine)

```
BATTLE_START
  → MOVE_SELECTION (both sides choose action: move / switch / item / run)
  → PRIORITY_RESOLUTION (determine action order: priority bracket > speed > random tiebreak)
  → ACTION_EXECUTION (loop per resolved action)
      → PRE_MOVE_CHECKS (sleep/freeze/paralysis/confusion checks, flinch)
      → MOVE_EXECUTION (accuracy check → effect application → damage calc → secondary effects)
      → FAINT_CHECK
  → END_OF_TURN (weather damage, status damage (burn/poison), held item effects, etc.)
  → FAINT_CHECK / SWITCH_PROMPT
  → BATTLE_END_CHECK (loop back to MOVE_SELECTION or end battle)
```

This skeleton should be built and tested with simple placeholder moves
(struggle-only or single tackle-like move) BEFORE any real move data is added.
Get the loop solid first.

## Build order (milestones — do not skip ahead)

1. **Data schema + state machine skeleton**
   Define `PokemonSpecies`, `MoveData`, `AbilityData`, `ItemData` resource
   classes. Build the `BattlePokemon` runtime class (species + current
   HP/stats/status/PP). Build the empty state machine above with one dummy
   move so two Pokémon can "battle" with no real mechanics yet — just to
   prove the loop works and a winner is declared.

2. **Damage formula + type chart**
   Implement the damage formula exactly as the expanded engine computes it
   (base power, attack/defense stat use via the Physical/Special split,
   STAB, type effectiveness, random factor, critical hits). Build the full
   18-type effectiveness chart (including Fairy). Verify against known
   damage calc examples from the source or community damage calculators
   that target this engine.

3. **Status conditions**
   Burn, poison, toxic, paralysis, sleep, freeze, confusion — implement each
   condition's application rules, turn-start/turn-end effects, and cure
   conditions, matching the expanded engine's specifics.

4. **Move effects — Tier 1 (simple damage)**
   Implement ~15–20 simple attacking moves with no secondary effect, across
   different types, to validate the damage/type system against real data.

5. **Move effects — Tier 2 (stat changes & status infliction)**
   Stat-modifying moves (Swords Dance, Growl, etc.) and status-inflicting
   moves (Thunder Wave, Toxic, etc.).

6. **Move effects — Tier 3 (multi-turn, recoil, drain, fixed damage)**
   Solar Beam / Dig / Fly style charge-and-release moves, recoil moves,
   drain moves (Giga Drain), fixed-damage moves (Seismic Toss, Dragon Rage).

7. **Move effects — Tier 4 (one-off / unique mechanics)**
   Counter, Destiny Bond, Metronome, Substitute, etc. — the long tail.

8. **Abilities**
   Tiered similarly: passive stat modifiers first, then switch-in effects,
   then complex interactions (weather-setting abilities, contact-based
   abilities like Static).

9. **Trainer AI**
   Port the expanded engine's actual rule-based decision logic from
   `battle_ai_script_commands.c` / `battle_ai_main.c`.

10. **UI / animation layer**
    Battle HUD, health bars, menu navigation, text box sequencing, basic move
    animations. Deliberately last — logic must be solid first so the UI has
    something correct to display.

    

Do not jump to UI or "make it look like the game" before the move/damage/
status core is working — visuals are the easiest part to get wrong-but-
convincing, which masks logic bugs.

## Build order — Phase 2 (post-core-engine expansion)

These milestones build on the complete M1–M10 core. Same rules: one milestone
at a time, verify against source, regression-sweep before moving on.

11. **Weather**
    Field-wide weather state (rain / sun / sandstorm / hail). End-of-turn tick
    following the same burn/poison trigger pattern established in M3. Damage
    modifiers in `DamageCalculator`: rain ×1.5 Water / ×0.5 Fire; sun the
    reverse; sand and hail deal end-of-turn chip to non-immune types. Un-stub
    the weather-setting abilities (`Drizzle`, `Drought`) left as placeholders in
    M8. Weather-aware AI scoring (explicitly deferred in M10's `decisions.md`).

12. **Held items**
    `ItemData` exists from M1 but is unpopulated. Build item mechanics before
    item AI: passive stat items (e.g. Choice Band); single-use consumables
    (berries triggered by HP thresholds, using the same trigger-shape as M8's
    contact abilities); choice-lock items (source already references
    `AI_DoesChoiceEffectBlockMove`, found during M10's source read).

13. **Item AI**
    Extend `TrainerAI` to consider held items in move scoring and switch
    decisions. Source: `battle_ai_items.c` (located but deferred during M10).
    This is a sequel to M10, not new architecture — requires M12 complete first.

14. **Doubles battle support**
    The largest item. Must not start until M11 and M12/M13 are stable and
    verified, since doubles must correctly interact with weather and items. Treat
    as three separate milestone passes with a full regression sweep after each —
    do not collapse into one combined effort.

    - **14a — State machine + turn order for 4 combatants:** spread/ally
      targeting changes core assumptions in `BattleManager`,
      `DamageCalculator`, and every move/ability that currently assumes a
      single attacker/defender pair. Fix the foundations here before touching
      move or AI logic.
    - **14b — Spread moves and ally-targeting effects:** move effects that hit
      multiple targets or interact with the ally slot.
    - **14c — Doubles AI:** source is `ChooseMoveOrAction_Doubles`,
      `AI_FLAG_DOUBLE_BATTLE`, `AI_DoubleBattle` / `AI_AttacksPartner` — all
      located but skipped during M10's source read.

## Working style / instructions for Claude Code

- Work one milestone at a time. Do not implement Tier 3 moves while Tier 1
  is still unverified.
- When implementing a move or ability, state which source file and function
  you're basing the behavior on.
- When a mechanic seems ambiguous or you're not fully certain of the engine's
  specific behavior, stop and ask rather than guessing — call out the
  ambiguity explicitly.
- Write unit-test-style verification scenes/scripts for damage calc and status
  logic as they're built (e.g. a debug scene that runs known move/stat
  combinations and prints expected vs. actual damage), since correctness here
  is the entire point of the project.
- Keep `docs/decisions.md` updated with any mechanic verified against source,
  especially when a constant or rule was ambiguous and required a judgment
  call — so we don't re-litigate it later.
- The scope is "what the expanded engine does," not "whatever I think would
  be cool." If you have an idea for a mechanic or balance change beyond what
  `pokeemerald_expansion` implements, flag it as an idea in `docs/` rather
  than just adding it.

### Testing convention: snapshot via signals, not post-battle state

  This bit M16c and M16d independently (see `docs/decisions.md`), so it's
  captured here as a permanent rule rather than something each milestone
  re-discovers.

  Any Pokémon whose only available move is a **repeatable** effect — a
  side-condition/hazard-setter that can legitimately be re-cast after its
  effect naturally expires or gets cleared, or a toggle like Trick Room — will
  keep re-triggering once the turn's action queue drains and auto-select
  falls back to `moves[0]`, for as many turns as the test battle runs. A test
  Pokémon with no other move WILL cast that move again.

  Consequence: any assertion about "what happened after one specific action"
  must **never** read state (`_side_conditions`, `trick_room_turns`,
  `attacker.stat_stages`, etc.) after `start_battle()` fully returns — by then
  the battle may have run many more turns than intended, re-triggering,
  re-expiring, or re-toggling the exact state under test. Instead, snapshot
  the state by connecting to the relevant signal (`screen_set`,
  `hazard_set`, `trick_room_set`, `move_executed`, etc.) and capturing the
  value at the precise moment the signal fires — guarded to the first
  matching occurrence if the signal could plausibly fire more than once
  during the battle.

  Applies to every future milestone that introduces persistent/toggleable
  battle state (side conditions, field conditions, per-Pokémon volatiles) —
  see `docs/decisions.md`'s `[M16c]`/`[M16d]` entries for worked examples of
  tests that broke this way and how they were fixed.

### Testing convention: lambda-captured scalars are snapshots, not references

  This has already caused real bugs in M16c, M16d, M16e, and M17a — treat it as
  a known, recurring risk from the first draft of any new test, not something
  to catch reactively after a false pass slips through.

  GDScript lambdas capture local variables **by value, at the moment the
  lambda is defined** — not by reference. If a test connects a signal to a
  lambda that reads a local scalar (`int`/`bool`/`float`/`String`), and the
  lambda body then assigns to that variable, the assignment mutates a private
  copy inside the closure. The outer variable the test later reads never
  changes.

  This is dangerous in exactly the same "silent until it isn't" way as the
  signal-snapshot pitfall above: the test still runs, still prints a result,
  and often still passes — a positive-case assertion checking "did this stay
  false" trivially passes even when the mechanism under test is completely
  disconnected, because the outer variable was never going to change anyway.
  The bug only surfaces when a negative-case or discriminator assertion needs
  the captured value to actually flip, which is exactly what happened in
  M17a's Rock Head recoil test: the positive case (no recoil should fire)
  passed for the wrong reason, and only the negative case (recoil SHOULD fire
  for a non-Rock-Head attacker) failed and exposed that the signal handler's
  mutation was never reaching the outer scope.

  Fix: wrap the value in a single-element `Array` and mutate/read index `0`.
  Arrays are captured by reference, so mutations inside the lambda are visible
  to code outside it that holds the same Array.

  ```gdscript
  # WRONG — outer `fired` never becomes true, no matter what the signal does.
  var fired := false
  bm.recoil_damage.connect(func(mon, amount): fired = true)
  bm.start_battle(atk, def)
  _chk("recoil fired", fired == true)   # false pass/fail regardless of behavior

  # RIGHT — Array wrapper is captured by reference.
  var fired := [false]
  bm.recoil_damage.connect(func(mon, amount): fired[0] = true)
  bm.start_battle(atk, def)
  _chk("recoil fired", fired[0] == true)
  ```

  Applies to every test that connects a signal to a lambda and expects the
  lambda to communicate a result back to the enclosing test function — see
  `docs/decisions.md`'s `[M17a]` entry for the worked example of a test that
  broke this way and how it was fixed.

### Testing convention: type immunity precedes ability logic

  This has already caused real bugs in M17b and M17c — treat it as a known,
  recurring risk from the first draft of any new test involving a damaging
  move, not something to catch reactively after a misleading pass or fail
  slips through.

  Type effectiveness is checked before any ability/item logic gets a chance to
  run (see `DamageCalculator.calculate`'s early-return on `effectiveness ==
  0.0`). If a test scenario picks an attacker/defender type pairing that is a
  flat 0× immunity — not just a resistance — for the move being used, the hit
  never connects, damage is always 0, and the ability or item under test never
  actually executes its logic. The scenario looks plausible (a Ghost-typed
  defender to test a Ghost-related damage modifier, a Ground-type move against
  a Flying-type/Levitate holder to test a stat interaction) but is untestable
  by construction.

  This is dangerous in exactly the same "silent until it isn't" way as the two
  conventions above: the test still runs, still produces a result, and can
  still pass or fail in a way that looks reasonable — an assertion comparing
  "modified damage" against "baseline damage" can trivially pass at `0 == 0`
  even though the mechanism under test never fired. The bug only surfaces when
  the assertion actually needs a nonzero difference to hold, which is exactly
  what happened in M17b's Purifying Salt test (a Normal-type defender used to
  measure Ghost-type damage halving — Normal-types are outright immune to
  Ghost-type moves, unrelated to Purifying Salt, so the measured damage was 0
  regardless of the ability) and M17c's Cursed Body integration test (a
  Ghost-type holder hit by a Normal-type move — the same flat immunity, this
  time blocking the hit-reactive dispatch from firing at all).

  Fix: before writing any test scenario involving a damaging move, explicitly
  check the attacker's move type against the defender's type(s) against this
  project's own `TypeChart` and confirm effectiveness is nonzero — don't rely
  on a type pairing "sounding" safe. Unless the mechanism under test is itself
  type-specific, default to a neutral (1×) matchup chosen to have no side
  effects on the mechanic being isolated — the same one-variable-at-a-time
  discipline already used throughout this project's test suites.

  Applies to every test that measures damage, a damage modifier, or a
  hit-reactive effect through an actual `DamageCalculator.calculate` call or a
  full battle — see `docs/decisions.md`'s `[M17b]` and `[M17c]` entries for the
  worked examples of tests that broke this way and how they were fixed.

### Testing convention: pairwise damage comparisons must force every RNG input

  Any test that asserts one damage value is greater than, less than, or equal to
  another (not just "damage > 0") must explicitly force BOTH
  `BattleManager._force_roll` AND `_force_crit` on every scenario being compared — not
  just the roll, and not just on one side of the comparison. A comparison with only
  partially-forced RNG will pass most of the time and fail intermittently whenever
  an unforced crit or roll variance closes or inverts the gap the assertion expects.

  This has caused two confirmed intermittent failures so far: `[M17n-2]`'s original
  damage comparison (forced roll but not crit) and `[M17l]`'s Friend Guard comparison
  (forced neither). Both were fixed by explicitly setting `_force_roll` and
  `_force_crit = false` (or specific values, as appropriate) on every scenario in the
  comparison before running the battle. Treat any new pairwise damage comparison as
  suspect until both are confirmed forced.

### Testing convention: manual assertion-total recounts must account for
### `scenes/battle/integration_test.tscn`'s different print format

  Root-caused during a dedicated diagnostic session (2026-07-06) after this exact
  24-assertion gap recurred at least three times (`[M17f]`, `[M17g]`, `[M17n-7]`) with
  every prior occurrence resolved by shrugging and adopting whichever fresh number
  turned up, without investigating why. It's not test flakiness and it's not a real
  regression — it's a blind spot in every ad-hoc recount script written so far.

  Every suite in this project prints its result in one of two formats:
  `"<suite_name>: N/M passed"` (the majority) or `"Results: N passed, M failed"`
  (`damage_test`/`move_test`/`stat_test`/`status_test`, an older style). **One file,
  `scenes/battle/integration_test.tscn` (24 assertions, added by the "Prompt 9"
  commit — predates the M1-M17 milestone-numbering convention entirely), prints a
  third, distinct format: `"Integration tests: N passed, M failed"`** (see
  `integration_test.gd`'s own `_ready()`). Any recount that pattern-matches only the
  two known formats — which is every recount performed so far, including the one that
  produced this session's own "2160" figure before this note was added — silently
  drops this file's 24 assertions from the total. Compounding the blind spot,
  `integration_test.tscn` has never been listed in this file's own "Current status" or
  "Verification scenes" sections, so nothing here points a reader toward checking for
  it specifically.

  The direction of the drift has flipped sign across occurrences depending on which
  informal method a given session happened to use — `[M17f]`'s recount was 24 HIGHER
  than the stale prior figure (that session's method happened to catch this file,
  correcting an even older undercount), while `[M17g]`'s and `[M17n-7]`'s recounts were
  each 24 LOWER than the documented total (those sessions' methods missed it). The
  suite itself has never regressed or flaked — `integration_test.tscn` has run clean
  at 24/24 every time it's been included in a sweep at all.

  **When manually recounting total assertions across all `.tscn` files, always
  include `integration_test.tscn`'s count explicitly** (read its own `N/M passed`-style
  line by eye, or extend any grep/regex to also match `"Integration tests: (\d+)
  passed"`) — do not trust a recount that only searches for the two documented
  formats. If a fresh recount disagrees with the carried-forward total by exactly (or
  close to) 24, check this file first before assuming generic documentation drift.

  A larger, not-yet-implemented follow-up worth Rob's prioritization: either
  standardize every suite's final print line on one format (`"<name>: N/M passed"` is
  the majority convention already), or write a single canonical counting script
  checked into the repo (e.g. `scripts/count_assertions.sh`) that every session runs
  instead of ad-hoc grep, so this class of drift structurally can't recur. Not
  implemented in this session — diagnostic only, per Rob's explicit instruction.

### Testing convention: baseline-total verification requires two independent
### methods, cross-checked, run twice — no single number is trusted on its own

  This specific number (total assertions across all `.tscn` suites) has now been
  gotten wrong at least six times in a row — `[M17f]`, `[M17g]`, the M17n-7 recovery
  session, the "2179" figure, a "2160, all formats counted correctly" claim, and a
  "2222, two formats counted" claim — each one asserted with apparent confidence and
  each one wrong in a way the next session had to re-discover. A dedicated
  verification session (2026-07-06) finally proved a number instead of asserting one,
  and that process is now the standard, not a one-off.

  `scripts/count_assertions.sh` is the canonical counting script (see the note above
  for the format-recognition bug it exists to fix). But **a script's own self-reported
  total is not sufficient evidence on its own**, given this exact number's track
  record — it must be cross-checked against a second, independently-derived method
  before being trusted as a baseline:

  1. Run `scripts/count_assertions.sh` against a fresh full sweep.
  2. Independently derive each file's true assertion count by instrumenting a
     **throwaway scratch copy** of its `.gd` file (never the real committed file) so
     every assertion helper (`_chk`/`_check_exact`/`_check_range`/etc. — confirm the
     actual helper name(s) per file, several older files use different names) prints
     on PASS too, not just FAIL, then running it and counting total PASS+FAIL lines
     directly. A plain static `grep`/regex count of assertion-helper call sites in
     source is **not sufficient by itself** — it systematically diverges from the
     true runtime count whenever a file contains an `if`/`else` branch where only one
     side's assertion(s) actually execute (dead-code guards like `if ability == null:
     _chk(...skip..., false)` are common in this codebase's older suites), or a loop/
     wrapper function that calls the helper a variable number of times from one
     static call site. Any static-count mismatch must be individually root-caused
     (which specific branch or loop, and why) before being dismissed — never averaged
     away or assumed to be a rounding artifact.
  3. Confirm both methods produce the exact same total.
  4. Re-run the full sweep from a clean process state (`pkill -9 -f "Godot.*--headless"`
     first) a second time and confirm the identical total both times.

  Only once all four steps check out cleanly should a total be stated as verified.
  The last confirmed-this-way total was **2246 across 41 `.tscn` files** (2026-07-06)
  — see `docs/decisions.md`'s `[M17n-7]` follow-up entry for the full per-file
  reconciliation. Any future session that needs to state a new baseline (after adding
  or removing a suite) must repeat this full process, not extrapolate from the prior
  confirmed number plus an assumed delta.

## Setup

```bash
# From project root
git clone https://github.com/rh-hideout/pokeemerald-expansion reference/pokeemerald_expansion
```

This reference clone is for reading only — never modify it, never build it,
it exists purely to look up exact source logic.

## Current status

- M1 (data schema + state machine skeleton): **COMPLETE** — 2026-06-24
- M2 (damage formula + type chart): **COMPLETE** — 2026-06-24, 24/24 tests pass
- M3 (status conditions): **COMPLETE** — 2026-06-24, 75/75 tests pass
- M4 (Tier-1 move pipeline + freeze-thaw hooks): **COMPLETE** — 2026-06-25, 45/45 tests pass
- M5 (move effects — stat changes & status infliction): **COMPLETE** — 2026-06-25, 78/78 tests pass
- M6 (move effects — multi-turn, recoil, drain, fixed damage): **COMPLETE** — 2026-06-25, 62/62 tests pass
- M7 (move effects — Tier 4 one-off/unique): **COMPLETE** — 2026-06-26, 86/86 tests pass
- M8 (abilities): **COMPLETE** — 2026-06-26, 59/59 tests pass
- M9 (switching mechanics): **COMPLETE** — 2026-06-26, 64/64 tests pass
- M10 (Trainer AI): **COMPLETE** — 2026-06-26, 26/26 tests pass
- M11 (Weather): **COMPLETE** — 2026-06-27, 64/64 tests pass
- M12 (Held Items): **COMPLETE** — 2026-06-27, 60/60 tests pass
- M13 (Item AI): **COMPLETE** — 2026-06-27, 40/40 tests pass (includes M10's 26 + 14 new)
- M14a (Doubles — state machine + turn order for 4 combatants): **COMPLETE** — 2026-06-28, 25/25 tests pass
- M14b (Doubles — spread moves and ally-targeting effects): **COMPLETE** — 2026-06-28, 46/46 tests pass (40 M14a + 9 M14b + B8 fixture fix; all prior suites green)
- M14c (Doubles — AI): **COMPLETE** — 2026-06-28, 46/46 tests pass (C1–C3 added; all prior suites green)
- M15 Task 3 (PP system): **COMPLETE** — 2026-07-01, 26/26 pp_test assertions; all M1–M14 still green
- M15 Task 5 (two-turn moves): **COMPLETE** — 2026-07-01, 32/32 two_turn_test assertions; all M1–M15T3 still green
- M16a (Tier A move effects — RESTORE_HP / FOCUS_ENERGY / GROWTH / OHKO): **COMPLETE** — 2026-07-01, 52/52 m16a_test assertions; all 637 prior assertions still green (689 total)
- M16b (Tier B move effects — MINIMIZE / DEFENSE_CURL / Stomp minimizeDoubleDamage / ROLLOUT (Ice Ball) / MAGNITUDE): **COMPLETE** — 2026-07-02, 55/55 m16b_test assertions; all 857 prior assertions still green (912 total). Note: Stomp's canonical move ID is 23, not 31 (31 is Fury Attack) — corrected after checking `constants/moves.h`; see decisions.md.
- M16c (Tier C move effects — REFLECT / LIGHT_SCREEN / AURORA_VEIL / Brick Break screen-break): **COMPLETE** — 2026-07-02, 60/60 m16c_test assertions; all 912 prior assertions still green (972 total). Introduces `BattleManager._side_conditions[side]`, the first per-side (not per-Pokémon, not per-battle) state — designed for reuse by Trick Room / entry hazards in M16d.
- M16d (Tier D move effects — entry hazards SPIKES / TOXIC_SPIKES / STEALTH_ROCK / RAPID_SPIN / TRICK_ROOM): **COMPLETE** — 2026-07-02, 71/71 m16d_test assertions; all 972 prior assertions still green (1043 total). Hazards reuse `_side_conditions[side]` (new layer-count/bool keys); Trick Room adds `trick_room_turns` as a genuinely per-battle field and is the first mechanic to alter turn order itself (inverts only the speed tiebreak within a shared priority bracket). New `AbilityManager.is_grounded()` helper for Spikes/Toxic Spikes immunity (Stealth Rock deliberately does not use it).
- M16e (Tier E move effects — PURSUIT / PAIN_SPLIT / CONVERSION / CONVERSION_2 / PSYCH_UP / Baton Pass extension): **COMPLETE** — 2026-07-02, 53/53 m16e_test assertions; all 1043 prior assertions still green (1096 total). Pursuit is the first mechanic to reorder turn order for a specific pair of battlers (strikes before an opposing switch resolves) rather than a global rule; reuses M16b's `power_override` for its doubled-power case. Conversion 2 confirmed from source to use the TARGET's last-used-move type, not "last hit the user" (contradicts the move's own flavor text at GEN_LATEST config). Psych Up confirmed to also copy the Focus Energy crit-boost volatile, not just the 7 stat stages. Baton Pass gained the one passable field it was missing (`focus_energy`, added in M16a after M9's original Baton Pass work). **M16 (Tiers A–E) is now fully complete** — 291 new assertions across the 5 sub-milestones, 1096 total across all 19 numbered suites. See `docs/decisions.md`'s consolidated `[M16]` summary entry for the full tier breakdown and the reusable patterns introduced along the way.
- M16 Milestone-End Review (targeted audit, not a new milestone): **COMPLETE, no fixes required** — 2026-07-02. Audited three risk areas: Baton Pass passable-volatiles completeness (no gap — `focus_energy` was the only omission and it was already fixed in M16e; every other M16a–M16e field correctly excluded per source), Conversion 2's last-used-vs-last-hit-by test coverage (implementation was already correct; a genuine testing gap existed and was closed with a direct-conflict discriminator test), and Trick Room × Pursuit turn-order integrity (the two mechanisms are structurally disjoint in `_phase_priority_resolution`'s comparator — no conflict found; added permanent cross-tier coverage). One real but out-of-scope latent bug was discovered and flagged (not fixed): Conversion/Conversion 2's `species.types` mutation is never reset on ANY switch-out (not just Baton Pass), unlike source where the active-battler struct is fully repopulated per switch — see `docs/decisions.md`'s `[M16 Review]` Area 1 for the recommended fix. New `m16e_test` assertions (53→56) plus new `m16_review_test.gd`/`.tscn` (8 assertions); 1107 total across 22 numbered suites, all green.
- Follow-up fixes (three independent small fixes, not a milestone): **COMPLETE** — 2026-07-02. (1) Chilan Berry — wired the previously-unreachable `TYPE_NORMAL` bypass in the resist-berry damage modifier (M12 decisions.md gap I2). (2) Heavy Duty Boots — new `ItemManager.is_hazard_immune()`, wired into `_apply_switch_in_hazards` as one shared gate across Spikes/Toxic Spikes/Stealth Rock, respecting the Toxic-Spikes-Poison-absorb-still-applies nuance (closes the gap flagged in M16d's decisions.md). (3) Conversion/Conversion 2 type-reset bug — found during the M16 Review turned out to be misattributed to the wrong source function (`RESTORE_BATTLER_TYPE` is Mimicry-specific, not general); the actual mechanism is switch-IN repopulation (`CopyMonAbilityAndTypesToBattleMon`/`Cmd_switchindataupdate`), so the fix is a new `BattlePokemon.original_types` cache restored via `BattleManager._reset_mon_type` at the same 5 switch-in call sites M16d's hazards use — not at switch-out as originally guessed. `item_test` grew 63→77, `m16e_test` grew 56→58; 1123 total across 22 numbered suites, all green. Air Balloon (a consumed-on-hit mechanic, a different shape of problem) and the rest of the held-item roster remain deferred to the future M18 milestone — not bundled into this session.
- M17 Recon + Signature-Ability Sweep (scoping only, not a milestone): **COMPLETE** — 2026-07-02. `docs/m17_recon.md` covers the full 1-318 ability range (151 roster-relevant + 166 full-scope extension), classifies each into 6 mechanic buckets, proposes M17a-n tier sequencing, and (in its Section 13 addendum) flags 23 additional legendary/mythical/Ultra-Beast-exclusive abilities Rob hadn't yet excluded (Air Lock/Rayquaza, Victory Star/Victini, the Necrozma-Solgaleo-Lunala constellation, the Regi-trio pair, Beast Boost/all 11 Ultra Beasts, Toxic Chain/the Loyal Three, etc.) plus a reconsideration flag on the Primal weather trio. Two small data-pipeline fixes applied during this pass: 6 (not 2) ability IDs had symbolically-defined enum values causing missing `.tres` placeholder files, all now generated via the new `scripts/gen_ability_placeholders.py`. Read this doc (all of it, including the Addendum and Section 13) before starting any M17 sub-tier — it is the locked scoping source of truth, not re-derived per milestone.
- M17a (Tier A move effects — damage-pipeline modifiers): **COMPLETE** — 2026-07-02. 32 abilities (Overgrow/Blaze/Torrent/Swarm, Marvel Scale, Compound Eyes, Battle Armor/Shell Armor, Multiscale, Filter/Solid Rock/Tinted Lens, Adaptability, Rock Head, Sniper, Toxic Boost, Sand Force, No Guard, Guts, Hustle, Heatproof, Iron Barbs, Fur Coat, Tough Claws, Steelworker, Steely Spirit, Battery, Power Spot, Rocky Payload, Ice Scales, Defeatist, Flare Boost) — Section 11's proposal re-derived against Section 13's exclusions first (removed Shadow Shield/Prism Armor/Neuroforce/Full Metal Body/Transistor/Dragon's Maw, all legendary-exclusive). Source investigation found FOUR distinct pipeline hook points were needed, not the one function the recon's shallow pass assumed — extended `attack_modifier_uq412` and `defense_damage_modifier_uq412` (new `effectiveness` param), added `attacker_post_effectiveness_modifier_uq412` and `move_power_modifier_uq412` (new base-power pipeline stage, mirrors M14b's Helping Hand insertion point), plus `blocks_critical_hit`/`blocks_recoil`/`bypasses_accuracy_check`/`accuracy_modifier_percent`. New `BattleManager._get_ally()` doubles-partner helper (reuses M14a's existing combatant layout, not new infrastructure) for Battery/Power Spot/Steely Spirit's ally-aura boost. New `m17a_test.gd`/`.tscn`: 83/83 assertions; caught and fixed a real Marvel Scale direction bug (returned ×1.5 instead of the correct reciprocal ≈×0.667) and a recurring GDScript lambda-scalar-capture test bug. 1206 total assertions across 23 numbered suites, all green. M17b next.
- M17b (Tier B move effects — stat-stage-system interactions, no new infrastructure): **COMPLETE** — 2026-07-02. 32 abilities (Steadfast, Anger Point, Simple, Download, Clear Body, White Smoke, Keen Eye, Hyper Cutter, Moxie, Unaware, Contrary, Defiant, Weak Armor, Moody, Big Pecks, Justified, Rattled, Flower Veil, Sweet Veil, Gooey, Stamina, Water Compaction, Berserk, Tangling Hair, Competitive, Cotton Down, Steam Engine, Pastel Veil, Thermal Exchange, Anger Shell, Purifying Salt, Supersweet Syrup) — Section 11's Bucket B proposal re-derived against Section 13's exclusions; corrected two gaps in the task's own exclusion transcription (Beast Boost excluded as UB-exclusive; Moxie included since it reuses M14b's existing `_last_attacker` kill-attribution rather than needing new infra). Guard Dog and Opportunist deferred (both need genuinely new infrastructure — a forced-switch ability gate, and threading a check through ~33 `apply_stat_change` call sites, respectively). Confirmed three distinct mechanic shapes: magnitude modifiers (Simple/Contrary, in `AdjustStatStage`), change-blocking gates (Clear Body/White Smoke/Hyper Cutter/Big Pecks/Keen Eye/Flower Veil, in `CanAbilityPreventStatLoss`/`AbilityPreventsSpecificStatDrop`), and reactive triggers hooking EXISTING M8/M11/M14b/M16d infrastructure rather than the stat-change pipeline itself. New non-contact-gated `AbilityManager.try_hit_reactive_effects` corrects a real over-generalization in the M8-era `try_contact_effects` comment (contact ≠ a blanket rule for the whole `ABILITYEFFECT_MOVE_END` dispatch — only true for M8's specific ability subset). `try_switch_in`/`try_end_of_turn` both changed return type `int` → `Dictionary` (breaking change, fixed the one affected M8-era `ability_test.gd` call site). New `m17b_test.gd`/`.tscn`: 109/109 assertions, negative case for every ability; caught one real implementation bug (Moody's forced-value pool validation) plus two test-only bugs (Anger Point's clamped-delta expectation, Purifying Salt's Ghost-immune-Normal-defender mismeasurement). 1315 total assertions across 23 numbered suites, all green.
- M17c (Tier C move effects — switch-in/turn-end triggers, no new field-state infrastructure): **COMPLETE** — 2026-07-02. 22 abilities (Sand Stream, Snow Warning, Rain Dish, Ice Body, Dry Skin, Hydration, Shed Skin, Healer, Truant, Poison Point, Poison Touch, Effect Spore, Cursed Body, Toxic Debris, Flower Gift, Slush Rush, Cheek Pouch, Ripen, Hospitality, Anticipation, Forewarn, Frisk) — Section 11's Bucket C proposal re-derived against Section 13's exclusions; corrected a gap in the task's own transcription (Spicy Spray excluded as Scovillain-Mega-exclusive, falling under the pre-existing "no Mega Evolution" scope note) and confirmed Solar Power/Poison Heal are deferred to M17d and Harvest is out of scope (needs new "last consumed berry" infra) per the recon's own tier proposal, not the task's "expected shape" hint. Dry Skin's Water-move absorb+heal half deferred (needs Bucket-E immunity+heal infra, shared gap with unimplemented Volt Absorb/Water Absorb); its Fire-damage-taken and end-of-turn heal/damage halves are wired. Truant is the first PRE-MOVE canceler this project adds via an ability rather than a status (new `BattlePokemon.truant_loafing`, slotted into `StatusManager.pre_move_check` at its exact source position). Flower Gift needed a new `DamageCalculator.defender_ally` parameter (the DEFENDER's doubles partner, distinct from M17a's attacker-side `ally`) and drops its Cherrim-Sunshine-form gate, matching the same precedent already set for the Primal weather trio. `try_end_of_turn`/`try_contact_effects`/`try_hit_reactive_effects`/`attack_modifier_uq412`/`defense_damage_modifier_uq412`/`effective_speed` all gained new trailing parameters (additive, no breaking changes this time). New `m17c_test.gd`/`.tscn`: 79/79 assertions; caught one real implementation bug (Flower Gift's ally-share was unreachable behind an incorrect `defender.ability != null` gate) plus one test-only bug (Cursed Body integration test's Normal-vs-Ghost type immunity mismeasurement, same pitfall class as M17b's Purifying Salt bug). 1394 total assertions across 24 numbered suites, all green.
- M17d (Weather-setter completions + Primal trio + multi-part abilities deferred from M17c): **COMPLETE** — 2026-07-02. 5 abilities (Solar Power, Poison Heal, Primordial Sea, Desolate Land, Delta Stream) — Section 11's M17d proposal re-derived against the current exclusions; corrected a stale recon-prose pairing (Orichalcum Pulse excluded as Koraidon-exclusive per Rob's updated legendary-exclusivity standard, despite Section 11 originally grouping it here alongside Hadron Engine) and confirmed Dry Skin already shipped in M17c. Harvest deferred again (still no "last consumed berry" tracking anywhere in `ItemManager`/`BattlePokemon` — genuinely new infra this otherwise-pure-reuse tier doesn't need for its other 5 abilities; natural bundling partner is Cud Chew whenever that tracker gets built). Solar Power is two-part (Sp. Atk ×1.5 in sun via `attack_modifier_uq412`; maxHP/8 self-damage in sun via `try_end_of_turn`, sharing Dry Skin's `SOLAR_POWER_HP_DROP`-labeled result key). Poison Heal inverts the existing `StatusManager.end_of_turn_damage` to return a **negative** (heal) value instead of duplicating poison-tick logic — a flat maxHP/8 heal regardless of poison vs. toxic, though the toxic counter still increments. Primordial Sea/Desolate Land reuse the ordinary `WEATHER_RAIN`/`WEATHER_SUN` constants directly (no separate "Primal" weather value, no Primal-item gate, per Section 8.5's explicit recommendation). Delta Stream needed a genuinely new `DamageCalculator.WEATHER_STRONG_WINDS` constant plus its type-effectiveness side effect (weakens super-effective hits against Flying-type defenders, checked per-type-component) wired into BOTH of this project's independent type-effectiveness computations (`TypeChart.get_effectiveness`'s early immunity/ability-gate check, gained a new `weaken_flying_se` bool param, and the actual per-type UQ4.12 damage-multiplier block in `DamageCalculator.calculate`) since they aren't unified into one function the way source's is. New `m17d_test.gd`/`.tscn`: 30/30 assertions, passed clean on the first run — every damage-calc scenario checked against `TypeChart.TABLE` directly first, per CLAUDE.md's type-immunity-precedes-ability-logic convention. 1424 total assertions across 25 numbered suites, all green. **M17e (Terrain system) is void** — Rob's locked-in scope decision (confirmed before M17a began) excludes all 10 terrain-reliant abilities (Electric/Psychic/Misty/Grassy Surge, Surge Surfer, Grass Pelt, Quark Drive, Mimicry, Hadron Engine, Seed Sower), so the Terrain system Section 11 proposed for M17e will never be built for this project — the letter is skipped, not renumbered, to avoid touching the already-shipped M17a-d labels/citations for no benefit.
- M17f (Trapping check — new infrastructure — + Shadow Tag/Arena Trap/Magnet Pull): **COMPLETE** — 2026-07-02. First genuinely new-infrastructure tier since M14a — every M17a-d tier before it was pure reuse. Source: `battle_util.c :: IsAbilityPreventingEscape` (L4917-4941), called only from two selection-time sites in `battle_main.c` (the wild-battle Run menu, the party-switch-menu's `B_ACTION_SWITCH` case) — `CanBattlerEscape`, the separate function backing forced switches/faint-replacement/Baton Pass, literally has "no ability check" per its own source comment, confirming trapping is a selection-time-only gate. New `AbilityManager.is_trapped(mon, live_opponents) -> bool` encodes: a global Ghost-type exemption from all three abilities (`B_GHOSTS_ESCAPE >= GEN_6`, unconditional at this project's GEN_LATEST config); Shadow Tag's mirror-match exemption when both sides have it (`B_SHADOW_TAG_ESCAPE >= GEN_4`, also unconditional here); Arena Trap gating on `AbilityManager.is_grounded` (reused directly from M16d); Magnet Pull gating on Steel-type. New `BattleManager._get_live_opponents` mirrors `_apply_switch_in_abilities`'s existing live-opponent loop shape. Wired into `_phase_move_selection` immediately after a queued/AI-chosen switch sets `_chosen_switch_slots` — a blocked switch falls back to the mon's first move, the same fallback expression already used elsewhere in that function. Forced switches, faint replacement, and Baton Pass are confirmed unaffected by construction (none of those three call sites ever reads `_chosen_switch_slots` or calls `is_trapped` — not a special-cased exemption, just architecturally disjoint paths), and trapping only ever reads the OPPONENT's side, never the holder's own. Shed Shell (source's one item-based exemption) isn't modeled — this project has no Shed Shell item anywhere, confirmed via grep. New `m17f_test.gd`/`.tscn`: 28/28 assertions (ability data; 12 direct `is_trapped` unit tests covering every positive/negative/exemption case including a dual Ghost/Steel mon proving the Ghost exemption overrides Magnet Pull's type gate; full-battle integration for the block itself, Arena Trap's Flying exemption, Magnet Pull's Steel-only gate, Roar/Baton Pass/faint-replacement all bypassing trapping, and the holder's own side switching freely). Total assertions across all 27 numbered suites: **1476**, all green (a direct recount during this sweep found the actual suite count was already 27, not 26 as M17d's "25 prior suites" line implied — a stale count, not a regression; every suite passed clean). See `docs/decisions.md`'s `[M17f]` entry for full source citations and the Step 0 exclusion cross-check.
- M17g (Ability-suppression plumbing — new infrastructure — Mold Breaker / Neutralizing Gas): **COMPLETE** — 2026-07-03. Second genuinely new-infrastructure tier in M17. Step 0 excluded Turboblaze (163)/Teravolt (164) as legendary-exclusive (Reshiram/Kyurem-White, Zekrom/Kyurem-Black — Section 13.1), the correction M17f's own "next tier" note flagged in advance, and deferred Mycelium Might (298, a genuine hybrid whose ability-ignore half fits this tier but whose turn-order-last half needs the not-yet-built Stall shape) — final list just Mold Breaker (104) and Neutralizing Gas (256). Confirmed from source that the two abilities have fundamentally different suppression shapes and implemented each faithfully rather than sharing one mechanism: Mold Breaker is attacker-scoped and move-scoped (`moldBreakerActive` is set only while processing the Mold-Breaker holder's own current move, per `battle_util.c` L9799-9802, and never suppresses its own wielder — `CanBreakThroughAbility`'s `battlerDef == battlerAtk` exclusion, L4822-4827), and only bypasses abilities source flags `.breakable = TRUE` (26 of this project's abilities qualify — see `AbilityManager.MOLD_BREAKER_BREAKABLE`); Neutralizing Gas is field-wide and holder-presence-scoped (`IsNeutralizingGasOnField`, L4794-4803 — suppresses every OTHER live battler's ability, never its own, for as long as it's on the field). New `AbilityManager.effective_ability_id(mon, ng_active, attacker)` is the single suppression-aware chokepoint (mirrors source's `GetBattlerAbilityInternal`) that ~30 `AbilityManager` functions, 6 `StatusManager` functions, and `DamageCalculator.calculate` were all rewritten to route through instead of reading `mon.ability.ability_id` raw — confirmed via a full grep sweep, zero raw reads remain outside the primitive itself. New `AbilityManager.is_neutralizing_gas_active(combatants)` / `BattleManager._is_neutralizing_gas_active()` check the field fresh at each call site. **Retrofitted during M17h**: the `MOLD_BREAKER_BREAKABLE`/`NEUTRALIZING_GAS_UNSUPPRESSABLE` hardcoded arrays mentioned below were replaced with direct `AbilityData.breakable`/`.cant_be_suppressed` field reads after discovering `AbilityData` already had these exact fields defined and unused — see M17h's entry and `docs/decisions.md`'s `[M17g]` addendum. A source-verified correction to this tier's own task brief: Neutralizing Gas DOES suppress trapping (Shadow Tag/Arena Trap/Magnet Pull all route through the same `GetBattlerAbility` chokepoint `IsAbilityPreventingEscape` uses), but Mold Breaker does NOT (trapping is checked only at selection time, entirely outside any move-processing window) — `is_trapped` gained an `ng_active` param but deliberately no `attacker` param, documented explicitly so this doesn't get "fixed" backward later. A pre-existing, unrelated dead-code gap was found and flagged (not fixed): Ripen's ability-aware resist-berry modifier is computed correctly but never actually called by the real damage pipeline, which hardcodes the un-doubled constant instead — predates M17g, out of scope for this tier. New `m17g_test.gd`/`.tscn`: 31/31 assertions across 9 sections (ability data; `effective_ability_id`/`is_neutralizing_gas_active` direct unit tests including the NG-suppresses-Mold-Breaker-itself recursive interaction and a fainted-holder negative case; Mold Breaker bypassing a defending Levitate holder's Ground immunity via direct `DamageCalculator` calls, both the bypass itself and confirmation it doesn't apply when the Mold Breaker holder isn't the actual attacker; full-battle Neutralizing-Gas-suppresses-Intimidate and stops-once-holder-leaves scenarios; the `is_trapped()` interaction in both directions; a negative control proving the "did not fire" assertions were real discrimination, not vacuous passes). A regression-baseline discrepancy was found, surfaced to Rob, and resolved before implementation began: this file's own last-recorded total (1478, from M17f's follow-up) and the task's expected baseline (1444) both disagreed with a fresh direct recount (1454, 0 failures) — Rob confirmed proceeding on the measured 1454 baseline; root cause of the historical drift wasn't further investigated (out of scope), matching M17f's own similar "stale count, not a regression" resolution. Total assertions across all 28 `.tscn` files: 1454 prior + 31 = **1485**, all green. See `docs/decisions.md`'s `[M17g]` entry for full source citations.
- M17h (Ability-copy/overwrite plumbing — new infrastructure — Trace / Mummy / Receiver / Power of Alchemy / Wandering Spirit / Lingering Aroma): **COMPLETE** — 2026-07-03. Third genuinely new-infrastructure tier in M17. Step 0 re-verified all 6 IDs against Section 13's exclusion sweep — none needed correction, unlike the M17f→M17g handoff. Traced each ability's exact mechanic from source and kept them as genuinely separate functions rather than one shared "copy" primitive: Trace (switch-in, copies a live opponent's ability — battle_util.c L2964-3000, 50/50 random pick between two eligible doubles targets or deterministic if only one is eligible); Receiver/Power of Alchemy (doubles-only ally-fainting-triggered copy — confirmed from source these share the EXACT SAME dispatch function, `battle_script_commands.c :: BS_TryActivateReceiver` L12946-12968, not separate implementations); Mummy/Lingering Aroma (contact → one-directional overwrite of the ATTACKER's ability — confirmed mechanically identical via the same shared switch-case block, battle_util.c L3859-3883); Wandering Spirit (contact → BIDIRECTIONAL swap with the attacker, both sides reassigned, the opposite direction from Mummy's overwrite). All four read/write the RAW `.ability` field, never the suppression-aware accessor — confirmed from source, meaning a copied/traced ability is unaffected by suppression at copy time; suppression is purely a separate runtime check applied whenever the ability is later consumed (tested explicitly). A source-verified correction worth flagging: Mummy/Lingering Aroma's exemption is checked via `cant_be_suppressed` on the ATTACKER's current ability, NOT `cant_be_overwritten` as the task's own framing assumed — `cant_be_overwritten` is actually consumed by Skill-Swap/Entrainment-style moves this project doesn't have. Mid-tier design correction: discovered `AbilityData` already had `cant_be_copied`/`cant_be_swapped`/`cant_be_traced`/`cant_be_suppressed`/`cant_be_overwritten`/`breakable` fields defined and fully supported by `gen_abilities.py`, entirely unused — rather than add hardcoded arrays for M17h's own exemption needs on top of M17g's two existing arrays, all five were migrated to read directly off each ability's own `.tres` resource (a pure refactor — M17g's `m17g_test` suite re-ran unchanged at 31/31 post-retrofit). New `BattleManager.ability_changed(pokemon, new_ability_id)` signal, mirroring M16e's `type_changed` shape. New `m17h_test.gd`/`.tscn`: 64/64 assertions across 10 sections, including the bidirectional-swap confirmation (both sides checked, not just one), the doubles-targeting determinism (caught and fixed a test-authoring bug where `force_pick_second` was passed into the wrong positional parameter, silently falling back to real RNG), and the copy-time-vs-suppression-time cross-tier interaction. `.tres` data: 6 new abilities added with real source-cited descriptions (all previously empty placeholders) plus the M17g-retrofit field additions on 27 existing abilities — 114 total `.tres` files. Full regression: all 27 prior suites unchanged and still passing. Total assertions across all 29 `.tscn` files: 1485 prior + 64 = **1549**, all green. See `docs/decisions.md`'s `[M17h]` entry (and the addendum on `[M17g]`) for full source citations.
- M17i (Switch-out trigger hook — new infrastructure — Regenerator / Natural Cure): **COMPLETE** — 2026-07-03. Fourth genuinely new-infrastructure tier in M17. Step 0 re-verified both IDs against Section 13's exclusion sweep — neither Regenerator (144) nor Natural Cure (30) needed correction. Resolved Section 11's own hedge about splitting this tier from Wimp Out/Emergency Exit's HP-threshold forced-self-switch: confirmed from source that Regenerator/Natural Cure are purely reactive (fire only once a switch-out is already underway) while Wimp Out/Emergency Exit would need a genuinely different switch-*initiating* mechanism — the split is correct, and Wimp Out/Emergency Exit stay out of this tier entirely (deferred to a possible M17i-2). Both abilities share one dispatch function in source (`battle_script_commands.c :: Cmd_switchoutabilities`, L9322-9372) — new `AbilityManager.try_switch_out(mon, ng_active)` mirrors this exactly, wired via `BattleManager._apply_switch_out_abilities` into all three sites that reach source's `switchoutabilities` call (`_do_voluntary_switch`, `_do_forced_switch_in`, Baton Pass's inline switch-out block) but deliberately NOT `_do_switch_in` (faint replacement never reaches that source call at all). A source-verified correction worth flagging: the natural framing "fires only on voluntary switches" is wrong — source's `BattleScript_RoarSuccessRet` reaches the same `switchoutabilities` call, so Roar/Whirlwind-forced switch-outs trigger both abilities too; the real gate is "left the field alive," not "switch was voluntary" (verified via a dedicated Roar-forced-switch test). Confirmed via the `[M17f]` trapping gate that a blocked voluntary switch never reaches the new hook (architecturally disjoint, not a special-cased exemption), and confirmed neither ability carries a `.cantBeSuppressed` flag in source, so Neutralizing Gas correctly suppresses both through the existing `effective_ability_id` chokepoint. Per the `[M17h]`-established dormant-field-check discipline, confirmed neither ability needed any new/existing `AbilityData` exemption field populated — no hardcoded-array or field retrofit was needed this tier. New `m17i_test.gd`/`.tscn`: 35/35 assertions (ability data; direct `try_switch_out` unit tests for both abilities including the Neutralizing-Gas-suppression no-op; full-battle voluntary switch, faint-replacement non-trigger, Roar-forced-switch trigger, Baton Pass trigger, `is_trapped()` interaction, and Neutralizing Gas suppression interaction). Total assertions across all 30 `.tscn` files: 1549 prior + 35 = **1584**, all green. See `docs/decisions.md`'s `[M17i]` entry for full source citations. Note: a real flaky-test bug was found and fixed in `m17i_test.gd` at the start of the `[M17j]` session (Section 9's `S9.02` read post-battle HP instead of snapshotting via signal — see `[M17j]`'s addendum in decisions.md); the 1584 total above already reflects the fix.
- M17j (Item-transfer primitive — new infrastructure — Pickpocket / Sticky Hold / Magician / Symbiosis): **COMPLETE** — 2026-07-03. Fifth genuinely new-infrastructure tier in M17. Step 0 re-verified all four IDs against Section 13's exclusion sweep — none needed correction (Pickpocket's ID is defined symbolically in source, independently recounted to confirm it resolves to 124). Traced four genuinely different shapes rather than one uniform "steal" ability: Pickpocket (defender-keyed reactive, steals the attacker's item on a contact hit — folded into the existing `try_contact_effects` dispatch), Magician (genuinely attacker-keyed — the opposite direction from every existing entry in `try_contact_effects`/`try_hit_reactive_effects` — steals the target's item on any damaging hit, contact not required; needed a new top-level `try_magician` called directly from `_do_damaging_hit`), Sticky Hold (a passive blocker, not a trigger — gates whether Pickpocket/Magician's steal is allowed), Symbiosis (doubles-only voluntary give, not a steal — fires when an ally's item is removed by any means). New shared primitive `AbilityManager._try_steal_item(stealer, victim, ng_active)` mirrors source's `StealTargetItem` and is the ONE place Sticky Hold's block lives, reused by both Pickpocket and Magician rather than duplicated; Symbiosis uses a separate one-directional "give" (mirrors `BestowItem`) since Sticky Hold does NOT gate a voluntary give. Confirmed all three transfer functions reuse the existing `BattlePokemon.held_item` field directly — no parallel item-tracking mechanism. Per the `[M17h]`-established dormant-field-check discipline, confirmed no `AbilityData` field exists for "item cannot be removed" (Sticky Hold's block is a direct ability-ID check, the same precedent `[M17f]`'s trapping check set) — but source-verified that Sticky Hold DOES correctly carry the *existing* `breakable=true` flag, traced through to confirm it has no reachable consumer among these four abilities' own dispatches (would only matter for a future Knock-Off/Thief-style move, none of which exist yet) — untested-but-implemented, not a silently-dropped check. New `BattleManager.item_transferred` signal and `_try_symbiosis` wrapper (called from `_consume_item`'s single existing choke point, plus directly after Pickpocket/Magician's steals). New `m17j_test.gd`/`.tscn`: 48/48 assertions (ability data; direct unit tests for all three trigger abilities including a real `DamageCalculator.calculate()`-driven type-immunity test for Magician; full-battle Pickpocket; full-battle Sticky-Hold-blocks-Pickpocket with a source-verified role correction — Sticky Hold belongs on the ATTACKER being stolen from, since Pickpocket's holder is always the one hit; full-battle Magician; a chained doubles scenario where Magician's steal triggers Symbiosis; Neutralizing Gas suppression of all three). A quick, targeted check of `switch_test.tscn`'s long-standing unresolved flakiness (piggybacking on the m17i_test.gd bug fresh in mind) found the same superficial shape in Section 5A's post-battle field reads but confirmed it's not actually exploitable given that test's move roster — checked and ruled out, not re-fixed. Total assertions across all 31 `.tscn` files: 1584 prior + 48 = **1632**, all green. See `docs/decisions.md`'s `[M17j]` entry for full source citations.
- M17k (Priority-move-block check — new infrastructure — Dazzling / Queenly Majesty / Armor Tail): **COMPLETE** — 2026-07-03. Sixth genuinely new-infrastructure tier in M17. Step 0 re-verified all three IDs (literal, not symbolic) against Section 13's exclusion sweep — none needed correction. Confirmed from source (`IsDazzlingAbility`, battle_move_resolution.c L1499-1509) — not assumed — that all three share the exact same dispatch, genuinely identical mechanic. New `AbilityManager.blocks_priority_move(defender, defender_ally, attacker, move, ng_active)` mirrors source's `CancelerPriorityBlock`: an EXECUTION-TIME gate (dispatched before `CancelerAccuracyCheck` in source's canceler chain — confirmed via `sMoveSuccessOrderCancelers`'s fixed ordering), not a selection-time block, so the move is chosen normally and then fails, mirroring this project's existing "chosen, then fails" pattern (Roar's `no_switch_target`) rather than a pre-move `move_skipped` cancellation. Gated on `move.priority > 0` only. Confirmed SIDE-WIDE from source (matches Queenly Majesty's own "targeting the user or an ally" framing): the check covers both the move's actual target and that target's doubles partner, not holder-only — verified with a dedicated full-battle doubles test where the direct target holds no ability at all but its ally holds Dazzling, and the priority move still fails. Confirmed the block never affects the holder's own outgoing priority moves. Confirmed via `[M16d]`/`[M16e]` cross-reference that this is a structurally different concern from turn-order/speed-sorting (Trick Room, Pursuit) — no changes to `_phase_priority_resolution`'s comparator were needed. Verified the source's field-targeting-move exclusion (Trick Room/hazards) is already non-applicable here since none of this project's three field-targeting moves has positive priority — a confirmed-non-applicable simplification, not an assumed one. All three abilities carry `breakable=true`, and — unlike Sticky Hold's non-applicable case in `[M17j]` — this one is genuinely, immediately reachable (attacker and Dazzling-family holder are always different battlers), so a Mold-Breaker-holding attacker correctly bypasses the block, verified with a dedicated unit test. Per the `[M17h]`-established dormant-field-check discipline, confirmed no `AbilityData` field applies (a direct ability-ID check, same precedent as `[M17f]`'s trapping and `[M17j]`'s Sticky Hold). New `m17k_test.gd`/`.tscn`: 26/26 assertions (ability data; direct unit tests including the side-wide/fainted-ally/Mold-Breaker-bypass/Neutralizing-Gas-suppression cases; full-battle priority-blocked, priority-zero-not-blocked, side-wide doubles, holder's-own-move-unaffected, and a negative control). Baseline reconfirmed exactly at 31 files/1632 assertions before starting (no drift this time). Total assertions across all 32 `.tscn` files: 1632 prior + 26 = **1658**, all green. See `docs/decisions.md`'s `[M17k]` entry for full source citations.
- M17l (Doubles-redirect/aura abilities — Lightning Rod / Storm Drain / Friend Guard / Telepathy / Propeller Tail / Stalwart): **COMPLETE** — 2026-07-03. Unlike every tier since `[M17f]`, this one's task explicitly asked to VERIFY rather than assume "no new infrastructure" — verified directly: none was needed. Step 0 found nothing to correct (first time since `[M17d]`) — all six literal IDs clean against Section 13. Two genuinely different mechanic shapes plus a separate pair: Lightning Rod/Storm Drain are redirect-TRIGGER abilities (defender-side) that both redirect a matching-type move onto themselves (doubles only) AND fully absorb + Sp.Atk+1 whenever hit directly or by redirect (two separate source functions, `CanAbilityAbsorbMove` + `HandleMoveTargetRedirection`); Propeller Tail/Stalwart are redirect-BYPASS abilities (attacker-side, the opposite direction) — confirmed genuinely identical from source, not assumed. Telepathy (a damage exemption when the target is the attacker's own ally — source-verified NOT gated on the move being a spread move specifically, despite the flavor text) and Friend Guard (×0.75 reduction for the defender when the defender's ALLY holds it, not the holder's own damage) are a separate, unrelated pair. Checked this project's EXISTING Follow Me/Rage Powder implementation (`[M14b]`) for redirect precedence, per the task's explicit instruction — confirmed from source that Follow Me takes precedence over the ability-redirect, and Propeller Tail/Stalwart bypass both identically; wired accordingly. **No new infrastructure**: every mechanic reuses an existing pipeline hook — `absorbs_move_type`/`blocks_ally_damage` slot into `DamageCalculator`'s existing early ability-immunity gate (alongside Levitate); Friend Guard reuses the existing `defender_ally` parameter (`[M17c]`'s Flower Gift); Telepathy reuses the existing `ally` parameter (`[M17a]`'s Battery/Power Spot/Steely Spirit) with zero new plumbing; the redirect functions extend the existing Follow Me block in `_phase_move_execution` rather than adding a parallel path. Confirmed Lightning Rod/Storm Drain/Friend Guard/Telepathy all correctly carry `breakable=true` and are genuinely, immediately reachable Mold-Breaker-bypass cases (verified with dedicated tests for both the absorb and the redirect); Propeller Tail/Stalwart correctly carry no such flag. New `m17l_test.gd`/`.tscn`: 45/45 assertions across 12 sections (ability data; direct unit tests for all four mechanic shapes including Mold-Breaker-bypass and Neutralizing-Gas-suppression; full-battle doubles redirect+boost for both Lightning Rod and Storm Drain; Lightning Rod's direct-hit absorb confirmed working in singles too; Telepathy blocking an ally's attack but not an opponent's; Friend Guard's deterministic damage-reduction proof; Propeller Tail bypassing Lightning Rod's redirect — the tier's key cross-ability interaction; a negative control). Caught and fixed a real test-authoring bug during this tier (same class as `[M17j]`'s `switch_test.tscn` finding): two negative "never hit" assertions read events accumulated across the WHOLE battle rather than the specific queued turn-1 action, and failed intermittently once the original redirect target fainted in later turns and the single-move attacker legitimately retargeted the ability holder directly — fixed by filtering to each attacker's first recorded event. Stable across 8 reruns post-fix. Baseline reconfirmed exactly at 32 files/1658 assertions before starting (no drift). Total assertions across all 33 `.tscn` files: 1658 prior + 45 = **1703**, all green. See `docs/decisions.md`'s `[M17l]` entry for full source citations, including a flagged finding for M17m: Volt Absorb/Water Absorb/Sap Sipper/Flash Fire/Motor Drive/Well-Baked Body all share the exact same source dispatch (`CanAbilityAbsorbMove`) this tier's `absorbs_move_type` already partially extended.
- M17m (Absorb-family abilities — Volt Absorb / Water Absorb / Sap Sipper / Flash Fire / Motor Drive / Well-Baked Body / Earth Eater / Dry Skin's water half): **COMPLETE** — 2026-07-04. Pre-recon (`docs/m17m_absorb_recon.md`) run first, then re-verified fresh at Step 0 per standing discipline. **Scope note**: the recon's original M17m proposal was ~15 abilities (also Wonder Guard, Scrappy, Overcoat, Normalize+the "-ate" family, Mind's Eye) — this session narrowed scope to just the absorb family; those five remain unimplemented and unscheduled (see `docs/decisions.md` [M17m]'s "Next tier" section — NOT covered by M17n either). All eight route through the same source dispatch (`CanAbilityAbsorbMove`) `[M17l]`'s Lightning Rod/Storm Drain already partially extended, but split into three genuinely different on-absorb effect shapes: heal maxHP/4 (Volt Absorb/Water Absorb/Earth Eater, plus Dry Skin's previously-deferred water half — the same divisor, confirmed distinct from Dry Skin's own end-of-turn /8 tick), stat-stage boost of VARYING magnitude (Sap Sipper Atk+1, Motor Drive Speed+1 — confirmed NOT Sp.Atk despite the type overlap with Lightning Rod — Well-Baked Body Def **+2**, the tier's flagged highest-risk detail), and a persistent flag with a delayed payoff (Flash Fire — new `BattlePokemon.flash_fire_active`, cleared by `_clear_volatiles` like `minimized`/`defense_curled`; its own later Fire-move ×1.5 boost wired into the EXISTING `attack_modifier_uq412`, the same function Overgrow/Blaze/Torrent/Swarm occupy — confirmed a separate source function from the absorb dispatch itself). **Cross-cutting design decision**: widened `AbilityManager.absorbs_move_type`'s return type from a bare `int` to a `Dictionary` (`{}`/`{"kind": "stat"/"heal"/"flag", ...}`) to express all three shapes through one function, matching source's single dispatch — a deliberate breaking change to `[M17l]`'s existing contract, with its one call site and six direct unit-test assertions updated in place (`m17l_test` reran unchanged at 45/45). A second, unavoidable existing-test update: `m17c_test.gd`'s Dry Skin water-move assertion directly encoded the old "not implemented, deferred" premise as a passing test — now permanently false, so it was updated in place to assert the new absorbed-hit reality (not duplicated elsewhere; `m17c_test` reran at 79/79). New `m17m_test.gd`/`.tscn`: 63/63 assertions across 17 sections (ability data; direct `absorbs_move_type`/`attack_modifier_uq412` unit tests per group incl. Motor Drive's not-Sp.Atk and Well-Baked Body's not-+1 discriminators; Mold Breaker bypass and Neutralizing Gas suppression per group; full-battle heal-delta tests for Volt Absorb (incl. a full-HP-still-absorbs case), Water Absorb, Earth Eater, and a DEDICATED Dry Skin water-absorb test kept separate from `[M17c]`'s existing coverage; full-battle stat-boost tests for Sap Sipper/Motor Drive/Well-Baked Body; full-battle Flash Fire absorb plus a direct `DamageCalculator` comparison proving the later ×1.5 boost is real; a negative control). One anomaly investigated and ruled out: `doubles_test.tscn` failed once (53/54) with zero references to any of this tier's abilities, then reran clean 6/6 times — the same pre-existing RNG-flakiness class already documented for `switch_test.tscn`. Total assertions across all 34 `.tscn` files: 1703 prior + 63 = **1766**, all green. See `docs/decisions.md`'s `[M17m]` entry for full source citations. **M17n (unique/grab-bag, ~50+ abilities) is next** — will need its own sub-tier-splitting pass before implementation, not attempted here; whoever picks it up should also re-decide the fate of the five abilities flagged above as unscheduled.
- M17n pre-recon (`docs/m17n_recon.md`, scoping only, not a milestone): **COMPLETE** — 2026-07-04. Re-derived the true remaining-ability count from scratch (the "318 − 61 excluded" figure some task framing assumed was documented turned out to only exist in a past git commit message, not in any actual project doc — re-verified it anyway and it checks out): 136 implemented, 61 excluded (itemized across 9 sub-groups: pre-decided/Tera/hack-custom/Commander/the 11-ability Mega-form-bound group/the 23-item Section 13.1 sweep/the 4 Mega-exclusive-only abilities/the 10 terrain-reliant abilities/Orichalcum Pulse), **121 remaining**. Folded in the five abilities `[M17m]` deferred (Wonder Guard/Scrappy/Overcoat/Normalize+the "-ate" family/Mind's Eye) into Group 5 rather than giving them a separate tier. Proposed an 8-group sub-tier split by mechanic shape, with a recommended sequencing order (Group 5 first as the M17m continuation, Group 8's 37-ability grab-bag last with a note to split it further later).
- M17n-1 (Group 1: status-immunity family + simple no-ops — Insomnia/Vital Spirit/Immunity/Limber/Water Veil/Magma Armor/Inner Focus/Own Tempo/Shield Dust/Leaf Guard/Early Bird/Aroma Veil/Soundproof/Bulletproof/Illuminate/Honey Gather/Oblivious/Cute Charm/Damp/Run Away/Pickup/Ball Fetch): **COMPLETE** — 2026-07-04. Ran FIRST among M17n's sub-tiers (ability-ID-group order) rather than the recon's own risk-based recommendation (Group 5 first) — a deliberate sequencing choice by the task, not an oversight. Four categories: genuine status-immunity abilities (12, extending `StatusManager.try_apply_status`/`try_apply_confusion`, the same shape `[M17b]`'s Purifying Salt/Sweet Veil/Pastel Veil already established); move-flag immunity (Soundproof/Bulletproof — a real Step-0 finding cut this tier's cost: `MoveData` already had dormant `sound_move`/`ballistic_move`/`biting_move`/`punching_move`/etc. fields since the original schema, so ZERO new `MoveData` flags were needed, just a new `AbilityManager.blocks_move_flag` wired before the accuracy check); documented cosmetic no-ops (Illuminate/Honey Gather, matching the Anticipation/Forewarn/Frisk precedent); and abilities confirmed genuinely out-of-battle-engine scope (Run Away/Pickup/Ball Fetch — deliberately given NO constant or `.tres` entry at all, unlike the no-ops). Two real findings beyond the recon's own scope: (1) all seven status/confusion-immunity abilities ALSO cure a matching pre-existing condition on switch-in (`TryImmunityAbilityHealStatus`, a genuinely separate trigger point from infliction-blocking, extending `try_switch_in` with two new result keys); (2) Inner Focus/Own Tempo/Oblivious fully block Intimidate's Attack drop under this project's GEN_LATEST config (`IsIntimidateBlocked`) — found while checking Oblivious's assumed-no-op scope, turned out to be a real, currently-buildable mechanic. A source-verified discrepancy was flagged (not silently resolved): Aroma Veil's AI-facing effect list includes Disable/Encore, but the actual pokeemerald_expansion execution commands for those two never check it (only Attract/Torment's own commands do) — implemented matching the AI's real-intended-behavior list, via a new `MoveData.blocked_by_aroma_veil` flag designed to auto-extend to Taunt/Torment/Heal Block later. Retroactively fixed a real pre-existing gap: Ice Ball's `ballistic_move` flag was cited in a comment since `[M16b]` but never actually set on the data dict. New `m17n1_test.gd`/`.tscn`: 75/75 assertions across 20 sections (ability data; direct unit tests per Category-A ability with a discriminating negative case; Leaf Guard's sun-gate; Early Bird's decrement-rate comparison; the switch-in-cure section; full-battle Intimidate-block/Aroma-Veil-block/Soundproof/Bulletproof tests; Mold Breaker bypass and Neutralizing Gas suppression per category; a negative control). Caught and fixed a real bug on the first run (73/75): the new move-flag-block signal emitted with `attacker` instead of `defender` (copied from `[M17k]`'s `blocks_priority_move` precedent, which itself uses `attacker` — but this codebase's OTHER defender-side blocks like `aroma_veil_blocked`/`immune` correctly use `defender`), fixed in the implementation to match that closer precedent. Baseline reconfirmed exactly at 34 files/1766 assertions before starting (no drift). Total assertions across all 35 `.tscn` files: 1766 prior + 75 = **1841**, all green. See `docs/decisions.md`'s `[M17n-1]` entry for full source citations.
- M17n-2 (Group 2: weather/evasion + speed family, plus Air Lock — Sand Veil/Snow Cloak/Swift Swim/Chlorophyll/Sand Rush/Air Lock/Cloud Nine/Sand Spit): **COMPLETE** — 2026-07-04. Air Lock confirmed as Section 13.1's established KEPT precedent (not itself excluded, despite the Rayquaza association) — re-read directly rather than assumed. Cloud Nine/Air Lock confirmed genuinely identical from source (the literal same `HasWeatherEffect` case branch, no asymmetry). Three shapes: weather-conditional Speed doublers (Swift Swim/Chlorophyll/Sand Rush, extending `[M17c]`'s Slush Rush precedent in `StatusManager.effective_speed` — a source-verified nuance NOT shared uniformly: Swift Swim/Chlorophyll additionally respect the HOLDER's own Utility Umbrella, since Umbrella only ever strips rain/sun, while Sand Rush/Slush Rush's sandstorm/hail conditions are untouched by it); weather-conditional evasion (Sand Veil/Snow Cloak, extending `[M17a]`'s Compound Eyes/Hustle precedent in `AbilityManager.accuracy_modifier_percent`, now also defender-aware); and a reactive weather-setter (Sand Spit, reusing the EXISTING `try_set_weather` Drizzle/Drought/Sand Stream already use). **The key design decision**: rather than retrofitting Air Lock/Cloud Nine awareness into every individual weather-conditional ability, new `BattleManager._effective_weather()` substitutes `WEATHER_NONE` at the small number of call sites that thread `weather` into a downstream ability function — covering `DamageCalculator.calculate` (Flower Gift/Solar Power/Dry Skin/Delta Stream, for free), `try_end_of_turn` (Rain Dish/Ice Body/Dry Skin heal, for free), the end-of-turn sandstorm/hail chip check, `try_secondary_effect` (Leaf Guard, for free), and `effective_speed`/`check_accuracy` (this tier's own abilities plus Slush Rush for free) — all without touching any of those functions' own code. Confirmed from source that weather-SETTING itself (Sand Spit, Drizzle, etc.) is unaffected by Air Lock/Cloud Nine — only weather's effects are negated — so `try_set_weather` was correctly left untouched. New `m17n2_test.gd`/`.tscn`: 41/41 assertions across 14 sections, including the tier's explicitly-requested key cross-ability test (Air Lock present negates Swift Swim's speed-doubling, restoring normal turn order despite rain being active) plus full-battle confirmation of Air Lock negating both the weather damage multiplier and end-of-turn chip damage. Caught and fixed a real flaky-test bug during reruns (not the first run): a full-battle damage-comparison test forced the damage roll but not the crit chance, so an occasional natural crit inflated damage above its forced-roll baseline — fixed by also forcing `_force_crit = false`; stable across 8 reruns after. Baseline reconfirmed exactly at 35 files/1841 assertions before starting (no drift). Total assertions across all 36 `.tscn` files: 1841 prior + 41 = 1882, all green. **Follow-up (2026-07-04, same day)**: the Air Lock/Sand Spit interaction — checked post-hoc via a manual scratch scene, never committed — was formalized into a permanent `m17n2_test.gd` Section 15 (5 new assertions, test-only, no production code changed): a 2-member-bench battle where the Air Lock holder's HP is tuned via a direct `DamageCalculator` probe to survive exactly 3 hits and faint on the 4th, checked via signal-ordering (not turn-counting) against a tagged event log. Confirms Sandstorm is genuinely set despite Air Lock's presence (weather-SETTING is confirmed from source to be unaffected by Air Lock/Cloud Nine — only the read side, `GetWeather()`, is filtered) and that chip damage resumes the moment Air Lock leaves the field. Reran 8 consecutive times, stable. Total assertions across all 36 `.tscn` files: 1882 prior − 41 (old `m17n2_test` count) + 46 (new count) = **1887**, all green. **Follow-up (2026-07-05, found during `[M17n-6]`'s Overcoat work, fixed here)**: this tier's original conclusion that Sand Veil/Sand Rush grant no sandstorm-chip immunity was WRONG — current source (`battle_end_turn.c :: HandleEndTurnWeatherDamage` L143-169) explicitly exempts Sand Veil/Sand Force/Sand Rush from sandstorm chip and Snow Cloak from hail chip, the same shape as Overcoat's own exemption. Fixed via a new `weather` param on `AbilityManager.blocks_weather_chip_damage` (the function `[M17n-6]` built for Overcoat); the stale code comment in `BattleManager._phase_end_of_turn` was corrected. New `m17n2_test.gd` Section 16 (12 new assertions, 58 total in the file): per-ability direct tests with a same-other-weather discriminator each, one Neutralizing-Gas-suppression test, and two full-battle confirmations (Sand Rush/sandstorm, Snow Cloak/hail) each paired with a plain-Pokémon control. Stable across 5 reruns; `m17n6_test.tscn` also rerun 5 times to confirm no regression in Overcoat's own weather-chip/Air-Lock composition test. Total assertions across all 40 `.tscn` files: 2172 prior + 12 = **2184**, all green. See `docs/decisions.md`'s `[M17n-2]` entry (including both follow-up subsections) for full source citations.
- M17n-3 (Group 3: turn-order/priority modifiers — Prankster/Gale Wings/Triage/Quick Draw/Stall/Mycelium Might): **COMPLETE** — 2026-07-04. Two shapes: priority-bonus abilities (Gale Wings +1 Flying-type-at-full-HP, Prankster +1 status-category, Triage +3 healing_move-flagged — new `AbilityManager.move_priority_bonus`, wired into `_phase_priority_resolution`'s `pa`/`pb` computation) and same-priority-bracket tiebreak abilities (Quick Draw 30% act-first on non-status moves, Stall always-last unconditionally, Mycelium Might always-last ONLY when its own chosen move is status-category — a source-verified nuance NOT shared with Stall — new `AbilityManager.quick_draw_activates`/`has_slow_turn_order_effect`, precomputed once per battler per turn into Dictionaries before the sort, checked strictly before the speed/Trick-Room comparison). Mycelium Might's second half (ignores the target's ability while using a status move) extends `effective_ability_id` with a new optional `attacker_move` param, threaded through `try_apply_status`/`try_apply_confusion`/`try_secondary_effect`; verified end-to-end bypassing Limber via Thunder Wave. Wired the previously-dormant `MoveData.healing_move` flag onto Recover/Slack Off/Heal Order (the only 3 of source's 24 flagged moves in this project's roster). **A real cross-tier gap was found and fixed**: `[M17k]`'s `blocks_priority_move` checked raw `move.priority` instead of the same ability-boosted priority that feeds turn-order sorting (source's `CancelerPriorityBlock` computes its priority via the identical `GetChosenMovePriority` function) — unreachable at `[M17k]`'s own implementation time, now fixed via the same `move_priority_bonus` helper; a null-attacker/null-mon crash surfaced by `[M17k]`'s own pre-existing sanity-check test was also caught and fixed with defensive null guards. Confirmed Trick Room's speed-tiebreak inversion never overrides the priority-bracket comparison (even ability-boosted) and confirmed Stall's slow-effect overrides Trick Room's own tiebreak preference too; confirmed Pursuit is structurally disjoint (no dedicated test needed). New `m17n3_test.gd`/`.tscn`: 54/54 assertions across 16 sections, stable across 8 reruns. Baseline reconfirmed exactly at 36 files/1887 assertions before starting (no drift). Total assertions across all 37 `.tscn` files: 1887 prior + 54 = **1941**, all green. See `docs/decisions.md`'s `[M17n-3]` entry for full source citations. **Follow-up (2026-07-04, same day)**: closed two open items before starting M17n-4. (1) A real gap — Prankster-boosted status moves must fail against a Dark-type target (Gen 7+, source: `BlocksPrankster`, battle_util.c L9234-9252) — was never addressed in the original entry; fixed via new `AbilityManager.blocks_prankster_move`, derived from the same (ability, move.category) facts `move_priority_bonus` already uses, no new stored flag needed. New `m17n3_test.gd` Section 17 (8 assertions, 62 total in the file), stable across 6 reruns. (2) Explicit sequencing comparison, re-verified against the current codebase rather than the recon's original framing: Group 7 (Color Change/Protean/Libero/Multitype/RKS System/Gorilla Tactics, 6 abilities) needs ZERO new infrastructure (confirmed `_set_mon_type`/`_reset_mon_type`/`original_types`/`is_choice_item` all still exist unchanged), while Group 4 (27 abilities) contains at least one genuinely new mechanism (the Ruin quartet's field-aura shape) alongside its otherwise-`[M17a]`-shaped abilities — Group 7 is the more natural next step, not Group 4; see `docs/decisions.md`'s `[M17n-3]` follow-up subsection for the full comparison. Total assertions across all 37 `.tscn` files: 1941 prior − 54 (old `m17n3_test` count) + 62 (new count) = **1949**, all green. **M17n-4 (Group 7: type-mutation/choice-lock cheap reuses, 6 abilities)** was queued next here but was **NOT implemented at the time** — see the `[M17n-5]` entry below, which found this gap still open at the start of its own session and proceeded past it per explicit instruction. **Resolved in a later session** — see the M17n-4 status line immediately below, inserted in its correct chronological position; the gap is now closed.
- M17n-4 (Group 7: type-mutation/choice-lock cheap reuses — Color Change/Protean/Libero/Multitype/Gorilla Tactics): **COMPLETE** — 2026-07-04. Inserted here in its correct chronological position even though implemented in a later session than `[M17n-5]` below — this tier was originally queued to run BEFORE `[M17n-5]` but didn't; the gap is now closed, not relitigated. 5 abilities, not 6 — RKS System (225) excluded per Rob's explicit decision (recorded in memory). Zero new `BattleManager` mechanisms: Color Change/Protean/Libero/Multitype all reuse the EXISTING `_set_mon_type`/`_reset_mon_type`/`BattlePokemon.original_types` infrastructure (`[M16e]`/follow-up-fixes), Gorilla Tactics reuses the EXISTING `BattlePokemon.choice_locked_move` field (`[M12]`). Color Change extends `try_hit_reactive_effects` (fires on any damaging hit, gated on not-already-that-type/not-Struggle/not-Stellar-Mystery; confirmed via source that a Ghost-vs-Normal immune hit — 0 damage — correctly never reaches it). Protean/Libero confirmed genuinely the SAME source function/condition (`ProteanTryChangeType`), not flavor-text twins; new `BattlePokemon.used_protean_libero` gate wired into `_phase_move_execution` right after the choice-lock block (matching `CANCELER_PROTEAN`'s early position in source, before accuracy/Protect checks) — source's own code comment says "once per Battle" but the flag lives in the same `Volatiles` struct that's memset at every switch-in, so it's operationally once-per-switch-in-stint, confirmed by tracing the actual clear mechanism rather than trusting the comment. Multitype wired into `_apply_switch_in_abilities` (switch-in only) reading a new `ItemManager.HOLD_EFFECT_PLATE`/`multitype_plate_type` (reusing `hold_effect_param` for the type, the same pragmatic field-repurposing already established for Resist Berry, since this project has neither Judgment nor Natural Gift to need the source field's literal purpose) — **a real correction to this tier's own inherited assumption**: confirmed via a full `TryBattleFormChange` call-site enumeration that `FORM_CHANGE_ITEM_HOLD` is an OVERWORLD-only trigger, never dispatched in-battle, so a mid-battle held-item change (Trick, Knock Off, this project's own `[M17j]` Pickpocket/Magician/Symbiosis) does NOT retype a Multitype holder — verified with a real Magician-driven theft integration test, not just asserted. Multitype's `cant_be_suppressed=true` (source-faithful) means Neutralizing Gas correctly does NOT suppress it, unlike the other four. Gorilla Tactics confirmed via source to share the LITERAL SAME `choicedMove` storage slot as an item-based choice lock (an OR condition, not two locks) — extended the existing gate directly; its physical-move base-power ×1.5 (`move_power_modifier_uq412`) is a different pipeline stage from Choice Band/Specs' attack-stat modifier, and composes multiplicatively to 2.25× with an actual Choice item exactly as source's own test confirms — verified with a dedicated integration test, no special-case stacking code needed since the two stages already multiply independently. New `m17n4_test.gd`/`.tscn`: 44/44 assertions across 9 sections (ability data incl. Multitype's full 5-flag set; Color Change incl. the deliberate Ghost-vs-Normal immune-hit negative case and a same-defender second-hit-different-type re-trigger; Protean direct-function unit tests plus a full-battle first-use/no-second-use pair and a switch-out/switch-in stint-reset proof; Libero confirming the identical mechanism and its own `"libero"` signal tag; Multitype's Plate-driven type, no-Plate default, and the key does-NOT-live-update Magician-theft integration test; Gorilla Tactics' lock/boost/Choice-Band-composition trio; Mold Breaker non-bypass and Neutralizing Gas suppression incl. Multitype's asymmetric exemption; a negative control). Caught and fixed one real test-authoring bug on the first run (43/44): the Color Change re-trigger test queued only 2 actions but asserted the resulting event array had exactly 2 entries; once the queue drained the battle continued and the attacker auto-selected its first move again, firing a 3rd event — the same "repeatable-effect auto-select" pitfall CLAUDE.md's snapshot-via-signals section already documents, fixed by checking only the first two recorded events. Stable across 6 reruns. Baseline reconfirmed exactly at 38 files/2027 assertions before starting (no drift; the documented `doubles_test.tscn` flake recurred once and reran clean immediately after — the same known non-blocking class). Total assertions across all 39 `.tscn` files: 2027 prior + 44 = **2071**, all green. See `docs/decisions.md`'s `[M17n-4]` entry for full source citations.
- M17n-5 (Group 4: damage-pipeline leftovers — Sturdy/Iron Fist/Technician/Reckless/Sheer Force/Analytic/Tangled Feet/Strong Jaw/Mega Launcher/Stakeout/Long Reach/Fluffy/Punk Rock/Sharpness/Slow Start/Serene Grace/Super Luck): **COMPLETE** — 2026-07-04. Ran instead of the still-outstanding M17n-4 (Group 7), per explicit instruction — see the flagged gap above. Two real discrepancies found and resolved at Step 0: the task's own count (19) didn't match its own 18-name enumeration (this project's re-derivation also lands on 18, not 19); of those 18, Skill Link (92) was further deferred — confirmed via grep that no multi-hit mechanic exists anywhere in this codebase's battle logic (`multi_hit`/`strike_count` are dormant `MoveData` schema fields only), so it has nothing to modify. **17 abilities actually implemented.** Mechanism shapes: attacker move-power modifiers (`move_power_modifier_uq412`: Iron Fist/Technician/Reckless/Sheer Force incl. its own secondary-effect suppression/Strong Jaw/Mega Launcher, new `pulse_move` flag/Punk Rock's own half/Sharpness/Analytic); attacker stat modifier (`attack_modifier_uq412`, new `defender` param: Stakeout, Slow Start's Atk half); defender damage modifiers (`defense_damage_modifier_uq412`: Fluffy — confirmed a contact Fire move triggers NEITHER of its two mutually-exclusive branches, netting ×1.0, not both stacking to cancel out — and Punk Rock's defense half); crit stage (`DamageCalculator._roll_crit`: Super Luck, additive with Focus Energy); accuracy (`accuracy_modifier_percent`: Tangled Feet, reusing Sand Veil/Snow Cloak's exact hook); a new shared `AbilityManager.move_makes_contact` helper (Long Reach, wired into the 3 real contact-flag read sites); the first "survive a lethal hit" mechanism this project builds (Sturdy, both the full-HP-gated endure half and the unconditional OHKO-block half); a new `slow_start_timer` volatile (Slow Start's Speed half + the 5-turn switch-in/end-of-turn lifecycle); and a secondary-chance doubler with an explicit 100% cap (Serene Grace). Also closed a real dormant-flag gap: `biting_move`/`slicing_move` existed in the `MoveData` schema but were never wired into `gen_moves.py` (same class of gap `[M17n-3]` closed for `healing_move`); `pulse_move` was genuinely new. No move in this project's current roster carries any of the three flags — Strong Jaw/Sharpness/Mega Launcher are tested via synthetic `MoveData`. New `m17n5_test.gd`/`.tscn`: 78/78 assertions across 20 sections, including two genuinely new testing patterns for this codebase — statistical-sample rate tests for Super Luck/Serene Grace (no prior suite tests a probabilistic rate directly) and a signal-snapshot fix for Sturdy caught on the very first run (post-battle-state would see the holder fainted, since Sturdy only protects the first lethal hit). Stable across 10 reruns. Baseline reconfirmed exactly at 37 files/1949 assertions before starting (no drift). Total assertions across all 38 `.tscn` files: 1949 prior + 78 = **2027**, all green. See `docs/decisions.md`'s `[M17n-5]` entry for full source citations, including the then-still-open Group 7 gap (**resolved** in a later session — see the M17n-4 status line immediately below, inserted in its correct chronological position between M17n-3 and this entry).
- M17n-6 (Group 5: type-effectiveness-pipeline leftovers — Wonder Guard / Scrappy / Overcoat / Normalize / Refrigerate / Pixilate / Galvanize / Aerilate / Dragonize / Liquid Voice / Mind's Eye, 11 abilities): **COMPLETE** — 2026-07-05, with a same-day follow-up adding Aerilate and Dragonize. The highest-risk remaining tier in all of M17, per both `[M17l]`'s and `[M17m]`'s own advance flags — required a genuine pipeline restructure, not just a new branch. Wonder Guard's check (`AbilityManager.blocks_non_super_effective_hit`) reads the FULL combined type-effectiveness multiplier AFTER `TypeChart.get_effectiveness` computes it in `DamageCalculator.calculate`, unlike every prior ability-immunity gate (Levitate/absorb-family/Telepathy), which are flat 0x-or-nothing checks positioned BEFORE that value exists; confirmed it still blocks fixed/level-damage moves (Night Shade etc., which carry the same `power=1` placeholder as source) while correctly exempting status moves (power=0) and Struggle (TYPE_MYSTERY). Scrappy/Mind's Eye's Ghost-immunity bypass is threaded as a new `bypass_ghost_immunity` param into BOTH of this project's independent type-effectiveness computations (`TypeChart.get_effectiveness` and `get_uq412`), mirroring the Delta Stream `weaken_flying_se` precedent; Mind's Eye's second, independent half (evasion-ignore) simply extends the pre-existing `ignores_defender_evasion_stage` function, whose own doc comment had already anticipated this. Overcoat is two independent halves (powder-move immunity via the existing `blocks_move_flag`/dormant `powder_move` flag; weather-chip immunity as a new per-mon exemption alongside Air Lock/Cloud Nine's field-wide negation, confirmed non-conflicting via a dedicated composition test) — a real discrepancy was found and initially FLAGGED, not fixed, here: current reference source lists Sand Veil/Sand Force/Sand Rush/Snow Cloak as weather-chip exemptions too, directly contradicting `[M17n-2]`'s already-shipped "neither grants chip immunity" finding (**since fixed** — see `[M17n-2]`'s own status line above and its decisions.md follow-up); also flagged (still not fixed): Overcoat's un-modeled Effect Spore interaction. Normalize/Refrigerate/Pixilate/Galvanize/Liquid Voice (plus, from the same-day follow-up, Aerilate and Dragonize) mutate the MOVE's own effective type — implemented via a shallow-duplicated `MoveData` (only `.type` overridden) substituted for `DamageCalculator.calculate`'s own `move` parameter, so every existing type-aware ability/item check downstream sees the mutated type for free, with zero changes to any of them; confirmed all six power-boosting members (including Normalize itself, symmetrically) share the exact same ×1.2/UQ4.12=4915 value, and that Normalize boosts literally every move it uses (even an already-Normal one) since its own branch is unconditional on the original type, unlike the "-ate" family's. A real test-authoring bug was caught on the first run: comparing a Pixilate'd/Liquid-Voice'd move's damage against a single-Normal-typed attacker confounds the power-boost measurement with a STAB loss (mutating away from the attacker's own type loses STAB) — fixed by using a dual-typed attacker so STAB holds constant across the comparison. **Follow-up (same day)**: Aerilate (184, Mega-exclusivity exclusion reversed by Rob) and Dragonize (312) added, reusing this tier's exact infrastructure — Dragonize's own ID was flagged BEFORE implementing, since direct source inspection shows it sitting in `pokeemerald_expansion`'s own cluster of 6 hack-project-only custom ability slots (`m17_recon.md` Section 8.3's original exclusion, independently re-confirmed: no `aiRating` field, flanked by two literal blank `"-------"` placeholder slots and two abilities whose source description is literally `"Unimplemented."`); Rob confirmed implementing it anyway as a deliberate override (Dragonize has since become real in a newer generation than this reference tree models). New `m17n6_test.gd`/`.tscn`: 101/101 assertions across 22 sections (95 original + 6 from the follow-up), stable across 8 reruns (original) and 5 reruns (follow-up). Baseline reconfirmed exactly at 39 files/2071 assertions before starting (no drift). Targeted regression (`ability_test`/`m17a_test`/`m17m_test`/`m17n2_test`) plus two full 40-file sweeps (original + follow-up) all passed unchanged (0 real failures — one grep false-positive on `stat_test.tscn`'s own "fail at +6 max"-style test labels, confirmed harmless). Total assertions across all 40 `.tscn` files: 2071 prior + 95 + 6 = **2172**, all green. See `docs/decisions.md`'s `[M17n-6]` entry (and its same-day follow-up subsection) for full source citations.
- M17n-7 (Group 6: item/berry interaction — Klutz / Unnerve / Gluttony / Unburden / Harvest / Cud Chew): **COMPLETE** — 2026-07-06, a recovery-and-completion session (a prior session's implementation work was found already in the working tree, uncommitted, with no test file or decisions.md entry — re-verified from scratch against `include/constants/abilities.h` and source rather than trusted at face value). Step 0 confirmed all six IDs clean; none carry `breakable`/`cant_be_suppressed`. Klutz (`ItemManager.effective_held_item`, the single held-item chokepoint every other function already routes through) and Unnerve (blocks ALL berry effects via source's single `ItemBattleEffects` dispatcher gate, not just resist berries — already correctly scoped in the inherited code) both verified correct as found. Gluttony confirmed a no-op for this project's entire current item roster (Sitrus's fraction is hardcoded to 2 regardless of ability), wired in generically per the Sticky-Hold precedent. Unburden's unconditional Speed ×2 (`battle_main.c` L4686-4687) verified wired into `StatusManager.effective_speed`, activated via `CheckSetUnburden`'s every-item-removal-site pattern. Harvest finally builds the "last consumed berry" tracker `[M17d]` deferred twice — `BattlePokemon.last_consumed_berry`, confirmed against `include/battle.h`'s `struct PartyState` to be genuinely switch-persistent (unlike Unburden/Cud Chew's own switch-cleared volatiles). **A real bug was found and fixed** (not merely inherited-and-confirmed like the rest of this tier): Cud Chew's re-trigger sets source's `overrideBerryRequirements` flag, which makes BOTH the HP-threshold check AND the Unnerve check return unconditionally-bypassed (`battle_util.c` L5465, L338-339) — the inherited `sitrus_berry_heal`/`lum_berry_cures` calls still applied both gates unconditionally even with `override_item` set. Fixed by gating both checks on `override_item == null`, preserving only the one source-verified exception (no heal at exactly full HP even under override). New `m17n7_test.gd`/`.tscn`: 62/62 assertions across 13 sections including a dedicated discriminating test for the bug fix (Section 10: the bypass itself plus a without-override negative control proving it wasn't vacuous). Two test-authoring bugs caught and fixed on the first run (59/62): an out-of-domain Gluttony fraction test, and hardcoded (rather than dynamically read) Speed values for the Unburden test. A baseline discrepancy was found during this session and initially "resolved" by adopting a fresh recount of 2160 — **that figure was itself wrong**, confirmed by a dedicated follow-up diagnostic session (2026-07-06) that root-caused the recurring drift: `scenes/battle/integration_test.tscn` (24 real, always-passing assertions) prints a third result format (`"Integration tests: N passed, M failed"`) that every ad-hoc recount so far — including this tier's own 2160 figure — failed to recognize, silently dropping its 24 assertions. See CLAUDE.md's testing-conventions section for the full mechanism and the standing rule this established. Full regression: all 40 prior files pass unchanged (`item_test.tscn`, whose call sites this session's fix directly touched, reran clean at 77/77). **VERIFIED: 2246 total assertions across 41 `.tscn` files**, confirmed via two independent methods (`scripts/count_assertions.sh` against a fresh sweep, and a per-file instrumented-source cross-check) and reproducible across two independent clean runs (2026-07-06). See `docs/decisions.md`'s `[M17n-7]` entry for full source citations.
- M17n-8 (Group 8, sub-tier 1: contact/faint-timing + reactive/one-off — Aftermath / Innards Out / Corrosion / Merciless / Opportunist): **COMPLETE** — 2026-07-06. Step 0 confirmed all five IDs clean against `include/constants/abilities.h`; Perish Body (blocked on the unimplemented Perish Song move) and Suction Cups (deferred) explicitly excluded from this sub-tier per Rob's instruction, not silently dropped. None of the five carry `breakable`/`cant_be_suppressed`. Aftermath/Innards Out share one mechanism (`AbilityManager.faint_retaliation_damage`, wired into `_phase_faint_check` alongside Destiny Bond/Moxie's existing `_last_attacker` killer-lookup) but differ in two source-verified ways: Aftermath requires contact and is blocked by any Damp holder on the field (new `is_damp_active`, NG-aware per source's `GetBattlerAbility`-routed `IsAbilityOnField`); Innards Out has neither restriction and deals the fainted mon's own HP immediately before the fatal hit (new `_last_attacker_hp_before`/`_last_attacker_move` companion trackers), not a fixed fraction — confirmed via a deliberate overkill full-battle test. Corrosion bypasses Poison/Steel-type poison immunity via the attacker's own ability (`bypasses_poison_steel_immunity`, mirroring Scrappy's `bypasses_ghost_immunity` precedent), closing a gap M3's own code comment flagged since this project's earliest milestone. **A second, genuinely independent bug was found and fixed** while writing Corrosion's full-battle test: `_phase_move_execution`'s own general "type immunity check for foe-targeting moves" ALSO blocks Toxic (Poison-type) against a Steel-type target (a real, separate 0x type-chart immunity, correct for cases like Thunder Wave vs. Ground) — entirely independent of the status-specific check Corrosion was wired into. Source confirmed `ABILITY_CORROSION` appears exactly once in the whole reference tree, and its failure branch reuses the same "doesn't affect" script a flat type immunity does — meaning real source resolves this through the ONE status-specific check with no separate downstream gate for this purpose. Fixed by threading the same Corrosion check into that general gate, scoped narrowly to Poison-type moves only. Merciless is a guaranteed (not stage-bonus) crit vs. a poisoned/toxic'd target, tested via a 30-trial statistical sample mirroring `[M17n-5]`'s rate-testing pattern. Opportunist copies an opponent's stat INCREASE only (never decreases, never self-triggers by construction — opponents-only scope) at the same primary move-driven stat-change site `[M17b]`'s Defiant/Competitive already occupy; deliberately not gated on `move.stat_change_self` (source cares which side rose, not whether the move was self-targeted) and confirmed no infinite-loop risk (the copy calls `apply_stat_change` directly, never re-entering the reactive dispatch). Known simplification, documented: wired into this one call site only, not retrofitted into every `apply_stat_change` call site — ability-driven raises (Moxie, Weak Armor, Download) and Baton-Pass/Psych-Up copies aren't covered. A real test-authoring bug (infinite-battle deadlock from a Swords-Dance-only moveset) was caught and fixed on the first run (55/58). New `m17n8_test.gd`/`.tscn`: 58/58 assertions across 11 sections, stable across 8 reruns. Full regression (direct foreground sweep, `pkill -9` first, `scripts/count_assertions.sh` against fresh output, re-run twice from a clean state): all 41 prior files pass unchanged, 0 real failures both runs. Total assertions across all 42 `.tscn` files: 2246 prior + 58 = **2304**, confirmed via `count_assertions.sh` and reproducible across both runs. See `docs/decisions.md`'s `[M17n-8]` entry for full source citations.
- M17n-9 (Group 8, "wide-but-shallow systems" — Magic Guard / Infiltrator / Magic Bounce): **COMPLETE** — 2026-07-06. Step 0 confirmed all three IDs clean against `include/constants/abilities.h` (Magic Guard=98, Infiltrator=151, Magic Bounce=156); Good As Gold explicitly excluded from this sub-tier per Rob's instruction even though the recon's original text lists it alongside these three. Verified each ability's actual cost independently rather than trusting the recon's "cheap" framing — Magic Bounce needed genuinely new infrastructure, and this tier ended up touching more call sites than its name implied. Magic Guard's exemption list, verified per-site rather than assumed uniform: weather chip, status residual (burn/poison/toxic — toxic counter still ticks even though damage is zeroed), standard recoil moves (alongside Rock Head), Rough Skin/Iron Barbs' damage to the ATTACKER, Life Orb recoil, and Spikes/Stealth Rock switch-in damage — one reusable predicate (`AbilityManager.blocks_indirect_damage`) consulted at each of those six pre-existing call sites (no single shared chokepoint existed, and building one purely for this ability wasn't worth the blast radius). Deliberately NOT blocked, confirmed from source: Struggle's fixed recoil and `[M17n-8]`'s own Aftermath/Innards-Out retaliation against the killer (both gated only by other mechanisms in source, never Magic Guard) — plus Toxic Spikes' poison infliction itself (only the resulting residual damage is gated). Infiltrator bypasses screens (`GetScreensModifier`'s unconditional ×1.0 override) and Substitute (source's single shared `IsSubstituteProtected` chokepoint) for both damaging and status moves — one predicate (`bypasses_infiltrator_barriers`) threaded through all 5 existing substitute-check call sites plus the one screens computation; deliberately scoped narrower than source (no Mist/Safeguard, neither implemented here). Magic Bounce reflects the FIRST foe-targeting status move back at its own user — re-derived this project's exact bounceable subset directly from source's per-move `magicCoatAffected` flag (exactly 9 of 91 implemented moves qualify: Sand Attack, Tail Whip, Leer, Growl, Sleep Powder, Thunder Wave, Toxic, Confuse Ray, Will-O-Wisp; Stealth Rock is also flagged bounceable in source but hazard-setting's separate side-wide dispatch is a documented, deliberate scope gap for this tier). Implemented as a single non-recursive attacker/defender swap in `_phase_move_execution`, which gets "only one bounce ever" for free even in a Magic-Bounce-vs-Magic-Bounce matchup (tested explicitly). Confirmed from source and tested explicitly: Magic Bounce's check sits BEFORE the Dark-type Prankster-immunity gate, so a Prankster'd status move against a Dark-type Magic Bounce holder correctly bounces rather than being eaten as a no-op; `.breakable = TRUE` in source (the one exception among this tier's three abilities) — a Mold-Breaker attacker's status move is NOT reflected. New `m17n9_test.gd`/`.tscn`: 63/63 assertions across 13 sections, stable across 8 reruns. One real test-design bug was caught and fixed on the first run (61/63): the Magic-Bounce-vs-Magic-Bounce scenario originally gave both mons Growl, so each mon's own independent Growl also got reflected by the other's Magic Bounce — two legitimate but unrelated bounce events masquerading as "the same move bouncing twice." Full regression (direct foreground sweep, `pkill -9` first, `scripts/count_assertions.sh` against fresh output, re-run twice from a clean state, plus 5 extra reruns each of `m16c_test.tscn` and `tier4_test.tscn` given how extensively this tier touches screens/Substitute): all 42 prior files pass unchanged, 0 real failures across every run. Total assertions across all 43 `.tscn` files: 2304 prior + 63 = **2367**, confirmed via `count_assertions.sh` and reproducible across both clean sweeps. See `docs/decisions.md`'s `[M17n-9]` entry for full source citations.
- M17n-10 (Group 8, "unique/standalone" part 1 — Screen Cleaner / Liquid Ooze / Pressure / Quick Feet / Guard Dog / Forecast): **COMPLETE** — 2026-07-06, a recovery session after a prior session crashed mid-implementation. Step 0 re-verified everything from scratch rather than trusting the crashed session's own transcript: Liquid Ooze and Forecast turned out to already be fully and correctly implemented (confirmed independently against source), but none of the 6 abilities' `.tres` files had been regenerated yet — a real gap even in the "already done" abilities. Screen Cleaner, Pressure, Quick Feet, and Guard Dog had only ability-ID constants, zero logic. Screen Cleaner reuses Brick Break's exact both-sides screen-clear-and-signal shape from `[M16c]`. Pressure's new `AbilityManager.pressure_pp_cost` covers the doubles-spread edge case confirmed from source (a spread move vs. two Pressure holders costs 3 PP, not 2) and the TARGET_OPPONENTS_FIELD hazard exclusion; `BattlePokemon.use_pp` gained an `amount` param. Quick Feet closes the M3-era decisions.md S19 known gap (which itself cited the wrong ability_id, 7 instead of 95, corrected in place) — a paralyzed Quick Feet holder's speed is REPLACED (×1.5), never stacked (×1.5×0.5), with the halving. Guard Dog is genuinely TWO independent mechanics: an Intimidate-reversal half (`try_switch_in`'s new `opponent_guard_dog_change` key, reusing Defiant's `BattleScript_DefiantActivates` shape and the same -6-floor no-op gate `[M17b]` established) and a completely separate forced-switch-block half (new `AbilityManager.blocks_forced_switch`, wired into the Roar/Whirlwind block). A genuine mid-session correction, caught only because writing the test for it exposed the scenario was physically impossible to construct: Guard Dog's `.breakable=true` flag does NOT make the Intimidate-reversal half Mold-Breaker-aware — traced `moldBreakerActive`'s own source set-site and confirmed it's never active outside a move-processing window, which a switch-in trigger structurally isn't (same reasoning `[M17f]`/`[M17g]` already established for trapping's attacker-less `is_trapped()`); `.breakable` only actually matters for the forced-switch-block half. New `m17n10_test.gd`/`.tscn`: 59/59 assertions across 11 sections, stable across 8 reruns; caught and fixed two other test-authoring bugs along the way (a `_make_mon` HP-vs-base-stat parameter confusion breaking 3 Pressure PP tests, and an unwitting reliance on paralysis's own uncontrollable 25% full-para RNG making the Quick Feet turn-order test flaky — fixed by testing with poison instead, which triggers the same status-agnostic boost). Full regression: all 43 prior files pass unchanged, 0 failures; `count_assertions.sh` run twice from independently clean states with identical totals; `m16c_test.tscn`/`pp_test.tscn` each rerun 5x given this tier touches both directly. Total assertions across all 44 `.tscn` files: 2367 prior + 59 = **2426**, confirmed via `count_assertions.sh` and reproducible across both clean sweeps. See `docs/decisions.md`'s `[M17n-10]` entry for the full recovery-session writeup and source citations.
- M17n-11 (Group 8, "unique/standalone" part 2 — Comatose / Costar / Wonder Skin / Mirror Armor): **COMPLETE for these 4 abilities** — 2026-07-06. **Correction to this tier's own task framing, caught during mandatory Step 0 context-gathering before implementation began: M17n-11 is NOT the final Group 8 sub-tier and M17 is NOT fully closed by it** — see `docs/decisions.md`'s `[M17n-11]` entry, "M17 status" subsection, for the full reconciliation; do not treat M17 as done. Comatose's real source mechanic touches ~7 call sites (Sleep Talk/Snore/Rest/Nightmare/Wake-Up-Slap), but a full roster grep confirms NONE of those moves exist in this project — the only implementable piece is full non-volatile-status immunity, confirmed the exact same source case branch as Purifying Salt (`[M17b]`), NOT breakable, `cant_be_suppressed=true`. Costar reuses `[M16e]`'s Psych Up stat-stage-array-copy shape directly, gated on the ally having anything worth copying (a confirmed no-op otherwise, not a reset). Wonder Skin floors a status move's own accuracy stat to 50 (not a flat final-chance override) before the stage-ratio multiplication, confirmed via 2000-trial statistical tests (no deterministic accuracy-roll seam exists in this codebase). Mirror Armor redirects a non-self-inflicted stat decrease onto its source (NOT reversed in sign, unlike Guard Dog's own +1 reversal) at two independent call sites — confirmed self-inflicted drops are never redirected (tested via a synthetic move, since this roster has no Overheat/Draco-Meteor equivalent) — and confirmed architecturally independent from `[M17n-10]`'s Guard Dog via a dedicated doubles test (a single mon can only hold one ability, so the two never compete for the same Intimidate event); like Guard Dog, its Intimidate-specific redirect is NOT Mold-Breaker-aware (switch-in triggers are outside any move-processing window), while its move-based redirect correctly is. New `m17n11_test.gd`/`.tscn`: 51/51 assertions across 11 sections, stable across 8 reruns; two GDScript multi-line-lambda parse errors caught and fixed before the first real run. Full regression: all 44 prior files pass unchanged, 0 failures; `count_assertions.sh` run twice from independently clean states with identical totals; `m17n1_test.tscn`/`m17n10_test.tscn` each rerun 5x. Total assertions across all 45 `.tscn` files: 2426 prior + 51 = **2477**, confirmed via `count_assertions.sh` and reproducible across both clean sweeps. **M17 status, definitively reconciled the same day (see `[M17 Final Reconciliation]` in decisions.md and the new `docs/m17_final_ledger.md`)**: this tier's own guess of a ~12-ability unreconciled gap was itself incomplete — the real picture is **226 implemented + 91 excluded + exactly 1 open (Stench, ability 1) = 318**, verified programmatically with zero overlaps and zero gaps. The 91 exclusions include 24 that were already sitting in this file's own decisions.md (across `[M17n-4]`-`[M17n-9]`'s Step-0 sections) but never cross-referenced here, 3 abilities (Run Away/Pickup/Ball Fetch) confirmed out-of-battle-engine scope back in `[M17n-1]` but never added to any exclusion tally, and 5 (Skill Link, Good As Gold, Illusion, Perish Body, Suction Cups) Rob confirmed excluded directly in the reconciliation session. Stench (1) has no blocker at all — it simply fell out of `docs/m17n_recon.md`'s own Group 1-8 planning prose (that document's Group 4 header claimed 27 abilities while only naming 26; Stench was the uncounted 27th). **`docs/m17_final_ledger.md` is now the single source of truth for every ability's status — consult it before starting any future ability work, and update it in place rather than re-deriving a count from scratch. M17 will be fully closed the moment Stench is either implemented or explicitly excluded — that is the only remaining step, not a new tier or a scope conversation.**
- M17.5 Immunity Audit (recon: `docs/m17-5_recon.md`, batch fix: `[M17.5 Batch Fix]` in decisions.md): **RECON COMPLETE, ALL 5 FINDINGS FIXED** — 2026-07-06, same day. Recon systematically audited every canonical type-based and move-flag-based immunity in the engine (prompted by two immunity gaps found by accident during M17n) and found 3 real currently-reachable bugs, 1 narrower flagged-but-unfixed interaction, and 1 style inconsistency — full detail and per-category summary counts in `docs/m17-5_recon.md`'s Section D/E. All five were implemented same-day in the `[M17.5 Batch Fix]` session: Scrappy now blocks Intimidate (a `[M17n-1]` TODO never followed up after `[M17n-6]` implemented the ability); Grass-type now has general powder-move immunity (`AbilityManager.blocks_move_flag`'s new type-based branch, alongside Overcoat's pre-existing ability-based one); Sun weather now blocks Freeze for any type (a stale M3-era "not in scope" deferral, closed now that weather exists); Overcoat now blocks Effect Spore's proc against an Overcoat-holding attacker (corrected from the recon's imprecise framing — it's the attacker's own Overcoat, not the Effect Spore holder's); Tough Claws refactored onto the shared `move_makes_contact()` wrapper (style-only, verified behavior-unchanged). No new ability implementations, no scope/exclusion decisions changed. Total assertions across all 45 `.tscn` files: 2477 prior + 12 new (Fix 1: +1, Fix 2: +6, Fix 3: +3, Fix 4: +2) = **2489**, confirmed via `count_assertions.sh` run twice from independently clean states with identical totals; `m17n1_test.tscn`/`status_test.tscn`/`weather_test.tscn` each rerun 5x stable.
- M18 planning (`docs/m18_item_ledger.md`, `docs/m18_subtier_plan.md`, scoping only, not a milestone): **COMPLETE** — 2026-07-07. 378 total held items catalogued, 165 INCLUDED (Gen I-IV blanket + Rob's 23-item Gen V+ override list), 213 EXCLUDED (structural Mega/Z-Crystal/Tera + unlisted Gen V+). The 150 not-yet-implemented INCLUDED items grouped into 24 proposed sub-tiers (M18a-M18x) by mechanic shape, cheapest/most-precedented first, mirroring M17n's Group 1-8 discipline. 5 items (confusion-nature berries) excluded by Rob as too similar to Sitrus Berry; 6 flagged genuinely blocked (3 Orbs — missing species; Destiny Knot — missing Attract; Grip Claw — missing binding-move mechanic; Loaded Dice — missing multi-hit mechanic), each with a "build the prerequisite mechanic first" recommendation rather than a partial item stub. `docs/m18_subtier_plan.md` is the single source of truth for M18's remaining sequencing — consult it before starting any future item tier.
- M18a (Type-boost held items — Charcoal family / Incenses / Silk Scarf / Fairy Feather / Plates, 40 items): **COMPLETE** — 2026-07-07. First and highest-leverage M18 tier: one new `ItemManager.move_power_modifier_uq412` dispatch (mirrors `AbilityManager.move_power_modifier_uq412`'s existing base-power-modifier shape from `[M17a]`, NOT `attack_modifier_uq412`'s Choice Band/Specs shape as the plan doc assumed before source verification — `HOLD_EFFECT_TYPE_POWER`/`HOLD_EFFECT_PLATE` share one case in `CalcMoveBasePowerAfterModifiers`, a different pipeline stage) applies a flat ×1.2 (`UQ_4_12(1.2)`=4915) to a matching-type move's power, confirmed uniform across all 40 items via direct source read (`I_TYPE_BOOST_POWER=GEN_LATEST` in this reference clone). New `HOLD_EFFECT_TYPE_POWER=43` constant (independently re-derived from the enum, cross-validated against every pre-existing constant in the file). Confirmed items have no `.tres` data-pipeline (unlike moves/abilities) — pre-existing gap, not a new one, flagged not fixed. New `m18a_test.tscn`: 40 tests (one per item, no loop consolidation), 160/160 assertions, all passing on first run against hand-derived expected values cross-checked against `item_test.gd`'s existing Occa Berry worked example. Full 45-file baseline sweep run once before any change (all passed, 0 failures, consistent with the prior 2489-assertion total); only the new suite re-run after (per this tier's explicit routine-tier scope — the full sweep is Rob's manual step). The "no `.tres` data-pipeline for items" gap flagged here was closed in a same-day follow-up — see the item-data-infrastructure entry immediately below.
- Item data infrastructure (`gen_items.py` + `ItemData`/`.tres`/`ItemRegistry`, cross-cutting, not tied to a specific M18 sub-tier): **COMPLETE** — 2026-07-07. Closes the gap M18a's own entry flagged. New `scripts/gen_items.py` mirrors `gen_moves.py`/`gen_abilities.py`'s exact structure (dict → `DEFAULTS` → `FIELD_ORDER` → `render()` → `main()`), populated with M18a's 40 items only (re-derived from M18a's actual implementation, cross-checked programmatically against zero mismatches) — every future M18 sub-tier must add its items here, not inline. New `ItemData.item_id` field (mirrors `AbilityData.ability_id`; `MoveData` has no equivalent — the two pre-existing patterns already diverged on this point, noted rather than silently picked). New `ItemRegistry.get_item(id)` mirrors `MoveRegistry`'s path-convention loader exactly. **Key finding: `ItemManager.move_power_modifier_uq412` had no inline per-item data table to migrate** — it's pure generic logic reading whatever `ItemData` a `BattlePokemon` already holds; the 40 items' field values lived only inside `m18a_test.gd`'s inline construction calls. "Migration" was therefore building the missing by-ID lookup capability, not moving existing dispatch code. Verified via a new `item_registry_test.tscn` (204/204 assertions: full data-integrity loop over all 40 `.tres` files, plus a behavioral-parity check proving a registry-loaded `ItemData` and an inline-constructed one produce IDENTICAL `move_power_modifier_uq412` output) — `m18a_test.tscn` itself was re-run UNMODIFIED and still passes 160/160, confirming zero behavior change. Next: M18b (berry/misc items on an existing exact dispatch — resist berries, status-cure berries, Oran Berry — 23 items) should add its items to `gen_items.py` from the start rather than constructing `ItemData` inline.
- M18b (Berry/misc items on existing dispatches — 16 resist berries, 6 status/confusion-cure berries, Oran Berry, 23 items): **COMPLETE** — 2026-07-07. First tier to use the new item-data pipeline from the start (63 total `.tres` files now: 40 M18a + 23 M18b). Resist berries plugged into `defender_item_modifier_uq412` with zero code changes, confirming the plan's cheapness claim for that family. **Correction found at Step 0: the 6 cure berries were NOT a zero-mechanism plug-in as planned** — each of Cheri/Chesto/Pecha/Rawst/Aspear has its own distinct `HOLD_EFFECT_CURE_*` source constant (not Lum Berry's `HOLD_EFFECT_CURE_STATUS`), and Persim Berry (confusion) is architecturally separate from the other 5 (clears `confusion_turns`, not `.status`). Two existing functions renamed and extended in place — `sitrus_berry_heal`→`hp_threshold_berry_heal` (now also covers Oran's flat-vs-Sitrus's-percent heal, `HOLD_EFFECT_RESTORE_HP` vs `HOLD_EFFECT_RESTORE_PCT_HP`, distinct constants; also fixed a confirmed pre-existing gap where Ripen never doubled Sitrus's heal) and `lum_berry_cures`→`status_cure_berry_cures` (now a `match` over 6 hold_effects) — plus one genuinely new `confusion_cure_berry_cures` for Persim. **Real regression caught pre-ship:** the rename broke two pre-existing test files (`item_test.gd`, `m17n7_test.gd`) that called the old names directly — fixed, both confirmed passing with IDENTICAL counts before/after (77/77, 62/62). New `m18b_test.tscn`: 104/104 assertions, 23 tests. Regression: `m18a_test.tscn` 160/160 and `item_registry_test.tscn` 204/204 both unchanged. Flagged (not fixed): the 15 M12-era items (Occa/Chilan/Lum/Choice Band/Sitrus/etc.) still predate the `.tres` pipeline entirely, inline-only — a future cleanup pass should migrate them into `gen_items.py` too. **M18c is BLOCKED** — per `docs/m18_subtier_plan.md`'s own dependency table, Lansat Berry needs M18e's crit-stage-bonus mechanism and Custap Berry needs M18l's turn-order mechanism, neither of which exists yet. Recommend **M18e (crit-stage item bonus, 2 items) or M18h (EV/Power-item Speed-halving family, 7 items)** as the actual next tier instead — both are self-contained with no cross-tier dependencies.

## Development workflow

Run a verification scene headless (from project root):

```bash
/home/rob/Godot_v4.3-stable_linux.x86_64 --headless --path . scenes/battle/SCENE.tscn
```

**Verification scenes:**
- `scenes/battle/battle_test.tscn` — M1 battle loop (Struggle to faint)
- `scenes/battle/damage_test.tscn` — M2 damage formula and type chart (24 tests)
- `scenes/battle/status_test.tscn` — M3 status conditions (75 tests)
- `scenes/battle/move_test.tscn` — M4 move registry, damage via loaded moves, freeze-thaw (45 tests)
- `scenes/battle/stat_test.tscn` — M5 stat stages, secondary effects, accuracy, flinch (78 tests)
- `scenes/battle/tier3_test.tscn` — M6 multi-turn, semi-inv, fixed damage, recoil, drain (62 tests)
- `scenes/battle/tier4_test.tscn` — M7 Substitute, Counter/Mirror Coat, Protect, Destiny Bond, Disable, Encore, Bide, Metronome (86 tests)
- `scenes/battle/ability_test.tscn` — M8 abilities: Huge Power/Pure Power, Levitate, Thick Fat, Intimidate, Drizzle/Drought stubs, Speed Boost, Static, Flame Body, Rough Skin, Synchronize (59 tests)
- `scenes/battle/switch_test.tscn` — M9 switching: BattleParty unit tests, voluntary switch, volatile clear, non-volatile persist, Intimidate on switch-in, Roar/Whirlwind forced switch, Baton Pass passable transfer, faint replacement, full-party-faint battle end (64 tests)
- `scenes/battle/ai_test.tscn` — M10 Trainer AI: type effectiveness scoring, KO preference, type immunity avoidance, wasted status avoidance, two-turn penalty, BASIC vs SMART tier, proactive switch (all-immune / has-bad-odds), faint replacement, full battle integration (26 tests)
- `scenes/battle/weather_test.tscn` — M11 Weather: Drizzle/Drought set weather, duration countdown, expiry, no-clear-on-switch, same-weather no-op, overwrite, rain/sun damage modifiers (discriminating composition test), sandstorm/hail chip immunity, modifier revert on expiry, AI weather-aware scoring (64 tests)
- `scenes/battle/item_test.tscn` — M12 Held Items: Choice Band/Scarf/Specs stat boosts, Life Orb damage + recoil, Sitrus Berry HP heal, Lum Berry status cure, Leftovers EOT heal, resist berry (Occa/Chilan), Utility Umbrella, choice-lock enforcement, Heavy Duty Boots hazard immunity (77 tests)
- `scenes/battle/ai_test.tscn` — M10+M13 Trainer AI: effectiveness scoring, KO preference, type immunity, status avoidance, two-turn penalty, BASIC/SMART tiers, switch decisions, faint replacement, full battle integration; M13 adds choice-lock, bad-lock switch, item-boost discrimination (40 tests)
- `scenes/battle/doubles_test.tscn` — M14a Doubles foundation: BattleParty active_indices API, 4-combatant setup, turn order by speed, full-side faint required to end battle, targeted moves hit correct slot, faint replacement (slot-specific), voluntary switch in doubles (25 tests)
- `scenes/battle/m16a_test.tscn` — M16a Tier A move effects: RESTORE_HP (Recover/Slack Off/Heal Order), FOCUS_ENERGY (crit stage +2), GROWTH (+1/+2 Atk+SpAtk, sun doubling), OHKO (Guillotine/Horn Drill/Fissure/Sheer Cold — level check, custom accuracy, type immunity, semi-inv bypass) (52 tests)
- `scenes/battle/m16b_test.tscn` — M16b Tier B move effects: MINIMIZE (+2 evasion, minimized flag gated on success), DEFENSE_CURL (+1 defense, defense_curled set unconditionally), Stomp minimizeDoubleDamage (exact ×2 post-roll modifier), ROLLOUT/Ice Ball (5-hit power doubling 30→60→120→240→480, Defense Curl doubling, interruption/miss resets), MAGNITUDE (weighted power table, power_override plumbing) (55 tests)
- `scenes/battle/m16c_test.tscn` — M16c Tier C move effects (screens): REFLECT/LIGHT_SCREEN (exact floor(dmg/2) per-category reduction, 5-turn duration, already-up no-refresh, doubles ⅔ reduction, switch persistence), AURORA_VEIL (hail gate, independent coexistence with Reflect/Light Screen, no double-stacking, reduces both categories), crit bypass, Brick Break (clears target's side pre-damage, own hit unaffected) (60 tests)
- `scenes/battle/m16d_test.tscn` — M16d Tier D move effects: SPIKES (opponent's-side 3-layer stacking, per-layer maxHP fraction, grounded/Levitate immunity, switch persistence), TOXIC_SPIKES (1-layer poison vs 2-layer toxic, Poison-type absorb, Steel-type immunity, ungrounded immunity), STEALTH_ROCK (type-effectiveness damage table, hits Flying-types unlike Spikes), RAPID_SPIN (clears one hazard in Spikes→Toxic Spikes→Stealth Rock order, fires even hitting a Substitute), TRICK_ROOM (exact speed-order reversal within a priority bracket, priority still overrides it, toggle-off, 5-turn expiry) (71 tests)
- `scenes/battle/m16e_test.tscn` — M16e Tier E move effects: PURSUIT (doubled power + turn-order interception striking the original switcher before its switch resolves, verified against direct `DamageCalculator` calls), PAIN_SPLIT (current-HP averaging both directions, floor rounding, Substitute block), CONVERSION (type from the literal first move slot, already-that-type failure), CONVERSION_2 (resist-type selection against the target's last USED move — not "last hit by" — exclusion-before-indexing, deterministic via `_force_conversion2_pick`, plus a direct-conflict discriminator against an earlier hit of a different type), PSYCH_UP (copies all 7 stat stages plus the Focus Energy volatile, overwrite semantics), Baton Pass (`focus_energy` now passes; `minimized`/`defense_curled`/`rollout_turns` still correctly excluded), type-reset-on-switch (a Conversion-mutated type reverts to the original species type after a voluntary switch-out and back in) (58 tests)
- `scenes/battle/m16_review_test.tscn` — M16 Milestone-End Review, Area 3: Trick Room × Pursuit turn-order integrity — Pursuit interception still fires correctly under Trick Room regardless of which side is naturally faster, doubled power and switch completion both still correct, and Trick Room's own speed-reversal is unaffected when a Pursuit-carrying Pokémon's target isn't switching (singles only — doubles combination explicitly flagged as untested in `docs/decisions.md`) (8 tests)
- `scenes/battle/m17a_test.tscn` — M17a Tier A move effects: 32 damage-pipeline-modifier abilities across attack-stat, defense-stat, post-type-effectiveness, and base-power hook points (Overgrow/Blaze/Torrent/Swarm, Marvel Scale, Compound Eyes, Battle Armor/Shell Armor, Multiscale, Filter/Solid Rock/Tinted Lens, Adaptability, Rock Head, Sniper, Toxic Boost, Sand Force, No Guard, Guts, Hustle, Heatproof, Iron Barbs, Fur Coat, Tough Claws, Steelworker, Steely Spirit, Battery, Power Spot, Rocky Payload, Ice Scales, Defeatist, Flare Boost), including the doubles-ally cases (Battery/Power Spot/Steely Spirit) and Guts' burn-halving exemption (83 tests)
- `scenes/battle/m17b_test.tscn` — M17b Tier B move effects: 32 stat-stage-system-interaction abilities across the three mechanic shapes — magnitude modifiers (Simple/Contrary), change-blocking gates (Clear Body/White Smoke/Hyper Cutter/Big Pecks/Keen Eye/Flower Veil), and reactive triggers (Defiant/Competitive, Weak Armor/Justified/Rattled/Anger Point/Berserk/Anger Shell/Steam Engine/Thermal Exchange/Cotton Down/Stamina/Water Compaction, Gooey/Tangling Hair, Steadfast, Download, Moody, Moxie, Sweet Veil/Pastel Veil/Purifying Salt status immunities, Supersweet Syrup's one-time gate), plus Unaware's 4 stage-ignoring touch-points, including full-battle integration tests (Growl+Defiant, Cotton Down, flinch+Steadfast, KO+Moxie) (109 tests)
- `scenes/battle/m17c_test.tscn` — M17c Tier C move effects: 22 switch-in/turn-end-trigger abilities — weather-setters (Sand Stream/Snow Warning), end-of-turn heal/cure (Rain Dish/Ice Body/Dry Skin/Hydration/Shed Skin/Healer), Truant's pre-move canceler + toggle cycle, contact status infliction (Poison Point/Poison Touch/Effect Spore's weighted roll), non-contact hit-reactive (Cursed Body/Toxic Debris) including full-battle integration, weather-conditional stat modifiers (Flower Gift incl. ally-share, Slush Rush), item-adjacent (Cheek Pouch/Ripen), Hospitality's switch-in ally heal, and the three cosmetic/no-op abilities (Anticipation/Forewarn/Frisk) (79 tests)
- `scenes/battle/m17d_test.tscn` — M17d weather-setter completions + Primal trio + multi-part abilities: Solar Power's two halves (Sp. Atk boost in sun + end-of-turn self-damage), Poison Heal's inversion of the poison/toxic end-of-turn tick (incl. the flat-not-counter-scaled heal and the still-incrementing toxic counter), Primordial Sea/Desolate Land's plain switch-in weather-setting with no item gate, and Delta Stream's Strong Winds weather plus its Flying-type-weakness-cancellation effect (mono-type and dual-type cases, plus negative cases for non-Flying defenders and sub-2.0x hits) (30 tests)
- `scenes/battle/m17f_test.tscn` — M17f trapping check: Shadow Tag/Arena Trap/Magnet Pull direct `is_trapped` unit tests (Ghost exemption, Shadow Tag mirror match, Arena Trap grounded-gate, Magnet Pull Steel-gate) plus full-battle integration (the block itself, Roar/Baton Pass/faint-replacement bypassing trapping, the holder's own side switching freely) (30 tests)
- `scenes/battle/m17g_test.tscn` — M17g ability-suppression plumbing: Mold Breaker/Neutralizing Gas — `effective_ability_id`/`is_neutralizing_gas_active` direct unit tests (incl. the NG-suppresses-Mold-Breaker-itself recursion and a fainted-holder negative case), Mold Breaker bypassing a defending Levitate holder's Ground immunity (and confirmation it doesn't apply when its holder isn't the actual attacker), full-battle Neutralizing-Gas-suppresses-Intimidate and stops-once-holder-leaves scenarios, the `is_trapped()` interaction in both directions (NG suppresses trapping, Mold Breaker does not), and a negative control (31 tests)
- `scenes/battle/m17h_test.tscn` — M17h ability-copy/overwrite plumbing: Trace (switch-in copy, `cant_be_traced` exemption, doubles 50/50 and single-eligible targeting), Mummy/Lingering Aroma (one-directional overwrite of the attacker on contact, already-holds-Mummy no-op), Wandering Spirit (bidirectional swap confirmed on both sides, `cant_be_swapped` exemption), Receiver/Power of Alchemy (doubles ally-faint copy, singles/non-holder/holder-itself-fainting no-ops, `cant_be_copied` exemption), full-battle integration for all four mechanisms, and the copy-time-vs-suppression-time cross-tier interaction (64 tests)
- `scenes/battle/m17i_test.tscn` — M17i switch-out trigger hook: Regenerator (floor(maxHP/3) heal, overheal clamp, Neutralizing Gas suppression) and Natural Cure (status cure incl. toxic_counter reset, volatile/confusion untouched, Neutralizing Gas suppression) direct unit tests, full-battle voluntary switch, faint-replacement non-trigger, Roar-forced-switch trigger (the source-verified "left the field alive, not voluntary" correction), Baton Pass trigger, and the `is_trapped()` interaction (35 tests)
- `scenes/battle/m17j_test.tscn` — M17j item-transfer primitive: Pickpocket (defender-keyed contact steal), Magician (attacker-keyed any-damaging-hit steal, incl. a real `DamageCalculator`-driven type-immunity test), Sticky Hold (blocks Pickpocket, source-verified attacker-role correction), and Symbiosis (doubles-only give, chained full-battle scenario off a Magician steal) — ability data, direct unit tests, full-battle integration, and Neutralizing Gas suppression for all three trigger abilities (48 tests)
- `scenes/battle/m17k_test.tscn` — M17k priority-move-block check: Dazzling/Queenly Majesty/Armor Tail (confirmed identical dispatch) blocking a priority-positive move (Quick Attack), not blocking priority-zero (Tackle), side-wide protection in doubles (ally holds it, direct target doesn't), the holder's own priority move unaffected, Mold Breaker bypass, Neutralizing Gas suppression, and a negative control (26 tests)
- `scenes/battle/m17l_test.tscn` — M17l doubles-redirect/aura abilities: Lightning Rod/Storm Drain (redirect + absorb + Sp.Atk+1, doubles and singles direct-hit, Mold Breaker bypass), Propeller Tail/Stalwart (bypasses the redirect — key cross-ability test), Telepathy (blocks an ally's attack, not an opponent's), Friend Guard (reduces the ally's damage, not its own), and a negative control (45 tests)
- `scenes/battle/m17m_test.tscn` — M17m absorb-family abilities: heal-on-absorb (Volt Absorb/Water Absorb/Earth Eater/Dry Skin's water half, exact maxHP/4 delta, full-HP-still-absorbs case), stat-boost-on-absorb (Sap Sipper Atk+1, Motor Drive Speed+1 — not Sp.Atk, Well-Baked Body Def+2 — not +1), persistent-flag-then-delayed-boost (Flash Fire absorbs and sets a flag, then a LATER own Fire-move gets a real, directly-verified x1.5 power boost), Mold Breaker bypass and Neutralizing Gas suppression per group, and a negative control (63 tests)
- `scenes/battle/m17n1_test.tscn` — M17n-1 status-immunity family + simple no-ops: genuine status/confusion-immunity abilities with discriminating negative cases (Insomnia/Vital Spirit/Immunity/Limber/Water Veil/Magma Armor), Leaf Guard's sun-gate, Own Tempo/Inner Focus's confusion/flinch blocks, Shield Dust's true-secondary-only gate, Early Bird's 2x sleep decrement, the switch-in status/confusion self-cure (a real finding beyond the recon), full-battle Intimidate-block (Inner Focus/Own Tempo/Oblivious), full-battle Aroma Veil blocking Disable, full-battle Soundproof/Bulletproof blocking Growl/Ice Ball, Mold Breaker bypass and Neutralizing Gas suppression per category, and a negative control (75 tests)
- `scenes/battle/m17n2_test.tscn` — M17n-2 weather/evasion + speed family, plus Air Lock: Swift Swim/Chlorophyll/Sand Rush speed-doubling with discriminators and the Utility Umbrella nuance (blocks Swift Swim/Chlorophyll, not Sand Rush), Sand Veil/Snow Cloak's accuracy reduction, full-battle Swift Swim changing turn order, the key cross-ability test (Air Lock negates Swift Swim's speed boost, restoring normal turn order), full-battle confirmation Air Lock negates both the weather damage multiplier and end-of-turn chip damage, Sand Spit setting Sandstorm on being hit (plus a no-duplicate-signal negative case), Mold Breaker bypass and Neutralizing Gas suppression, a negative control, and (added in a same-day follow-up) the Air Lock/Sand Spit interaction — Sandstorm is genuinely set despite Air Lock's presence and chip damage resumes the instant Air Lock leaves the field, checked via signal-ordering against a tagged event log (46 tests)
- `scenes/battle/m17n3_test.tscn` — M17n-3 turn-order/priority modifiers: `move_priority_bonus` direct unit tests for Gale Wings/Prankster/Triage (each with a discriminating negative case, plus null-mon/null-move guards), `quick_draw_activates`/`has_slow_turn_order_effect` direct unit tests (Quick Draw's forced-roll and status-move gate, Stall's unconditional shape vs. Mycelium Might's status-gated nuance), full-battle turn-order confirmation for all six abilities each with its own discriminator (Gale Wings' full-HP gate, Triage's narrower-than-Prankster healing_move gate, Quick Draw's forced-roll and status-move gate, Mycelium Might's own-move-category gate), Mycelium Might's ability-ignore half bypassing Limber via Thunder Wave with a plain-attacker negative control, Trick Room composition (priority bracket unaffected; Stall overrides Trick Room's own tiebreak preference), the `[M17k]` Dazzling-family composition tests (the real `blocks_priority_move` gap, its fix, and Stall's confirmed non-interaction), Neutralizing Gas suppression for all six, and a negative control, plus (added in a same-day follow-up) Prankster-boosted status moves failing against a Dark-type target with discriminators (non-Dark target, damaging move, non-Prankster attacker, null-move guard) and a full-battle pair (62 tests)
- `scenes/battle/m17n4_test.tscn` — M17n-4 Group 7 type-mutation/choice-lock cheap reuses: ability data spot-checks (incl. Multitype's full 5-flag exemption set), Color Change (type change on a real hit, a deliberate Ghost-vs-Normal immune 0-damage hit that must NOT fire it, a second different-type hit re-triggering without stacking, direct Struggle exclusion, negative control), Protean (direct `protean_new_type` unit tests incl. the already-used gate/Struggle/already-matching-type/null guards, full-battle first-use-changes-type-but-not-a-second-use pair, switch-out/switch-in stint-reset proof), Libero (identical-mechanism confirmation plus its own `"libero"` signal tag), Multitype (Plate-driven type at switch-in, no-Plate natural type, negative control, and the key does-NOT-live-update Magician-theft integration test), Gorilla Tactics (first-move lock, physical-only base-power boost with a special-move discriminator, Choice-Band 2.25x composition test), Mold Breaker non-bypass for a representative pair, Neutralizing Gas suppression incl. Multitype's asymmetric `cant_be_suppressed` exemption, and a negative control (44 tests)
- `scenes/battle/m17n5_test.tscn` — M17n-5 damage-pipeline leftovers: ability data spot-checks (17 implemented + Skill Link's confirmed-absent mechanism), Sturdy (survive-at-1HP via signal-snapshot, below-full-HP discriminator, OHKO block), each power/damage modifier with a positive case and a discriminating negative case (Iron Fist/Technician/Reckless/Sheer Force incl. its secondary-suppression half/Strong Jaw/Mega Launcher/Punk Rock's both halves/Sharpness), Analytic's full-battle moving-last confirmation with a moving-first discriminator, Fluffy's contact-Fire "neither branch fires" discriminator plus a Long-Reach interaction, Slow Start's direct Atk/Speed unit tests plus a full-battle 5-turn-timer confirmation, statistical-sample rate tests for Super Luck and Serene Grace (a new testing pattern for this codebase) plus Serene Grace's 100% cap, Mold Breaker bypass for the 4 genuinely-reachable abilities and Neutralizing Gas suppression for a representative sample, and a negative control (78 tests)
- `scenes/battle/m17n6_test.tscn` — M17n-6 Group 5 type-effectiveness-pipeline leftovers: Wonder Guard/Scrappy/Overcoat/Normalize/Refrigerate/Pixilate/Galvanize/Aerilate/Dragonize/Liquid Voice/Mind's Eye — see `CLAUDE.md`'s own M17n-6 status line above and `docs/decisions.md`'s `[M17n-6]` entry for the full breakdown (101 tests)
- `scenes/battle/m17n7_test.tscn` — M17n-7 Group 6 item/berry interaction: Klutz (held-item/choice-lock suppression at the single chokepoint), Unnerve (blocks ALL opposing berry effects, not just resist berries), Gluttony (fraction-widening, confirmed a no-op for this project's current item roster), Unburden (unconditional Speed ×2 once active, full-battle turn-order-flip integration), Harvest (end-of-turn berry regeneration, sun-guarantee, forced-roll seam, full-battle integration), Cud Chew (arm/fire one-turn cycle, plus a dedicated discriminating test for the override-bypass bug this session found and fixed — Cud Chew's re-trigger bypasses BOTH the HP threshold and Unnerve), Neutralizing Gas suppression for all six, and a negative control (62 tests)
- `scenes/battle/m17n8_test.tscn` — M17n-8 Group 8 sub-tier 1 contact/faint-timing + reactive/one-off: Aftermath/Innards Out (shared faint-retaliation mechanism — contact+Damp-gated fixed max_hp/4 vs. non-contact holder's-actual-remaining-HP, discriminated via a deliberate overkill scenario), Corrosion (Poison/Steel status-immunity bypass, plus the general type-immunity-gate fix this session found and fixed so Toxic can actually land on a Steel-type in a full battle), Merciless (guaranteed-crit statistical sample vs. a poisoned target), Opportunist (opponent stat-increase copy, self-trigger-safety discriminator, decrease/already-maxed no-ops), Neutralizing Gas suppression for all five, and a negative control (58 tests)
- `scenes/battle/m17n9_test.tscn` — M17n-9 Group 8 "wide-but-shallow systems": Magic Guard (all six exempted indirect-damage sources plus the toxic-counter-still-ticks nuance, full-battle integration with plain-mon discriminators throughout), Infiltrator (screens-bypass and Substitute-bypass full-battle tests, each with a plain-attacker discriminator), Magic Bounce (full-battle reflection with damaging-move-not-bounced and Encore-not-bounced discriminators, the Mold Breaker bypass scenario, the Magic-Bounce-vs-Magic-Bounce scenario, and the Prankster+Dark-type interaction with its own discriminator), Neutralizing Gas suppression for all three, and a negative control (63 tests)
- `scenes/battle/m17n10_test.tscn` — M17n-10 Group 8 "unique/standalone" part 1: Liquid Ooze/Forecast re-verification (both already implemented pre-session — drain inversion, weather-type mapping incl. mid-battle reversion on natural expiry), Screen Cleaner (both-sides screen clear on switch-in with a plain-Pokémon discriminator), Pressure (direct unit coverage of every branch — single-target, the doubles-spread edge case, hazard exclusion — plus full-battle integration incl. a real doubles Magnitude scenario), Quick Feet (paralysis-replace-not-stack, status-agnostic boost, a full-battle turn-order flip using poison to avoid full-para RNG), Guard Dog (both mechanically independent halves — Intimidate reversal with its floor-gate, and the forced-switch block with its own Mold Breaker bypass), Neutralizing Gas suppression for all 6, and a negative control (59 tests)
- `scenes/battle/m17n11_test.tscn` — M17n-11 Group 8 "unique/standalone" part 2: Comatose (full non-volatile-status-immunity coverage with a confusion-NOT-blocked discriminator, Mold-Breaker-non-bypass and Neutralizing-Gas-non-suppression confirmations), Costar (copy/no-op/singles-no-op direct unit tests plus a full-battle doubles switch-in), Wonder Skin (2000-trial statistical accuracy-floor tests — the floor itself, the damaging-move exemption, the already-≤50%-not-raised case via a synthetic move, and the Mold Breaker bypass), Mirror Armor (unit tests plus full-battle move-based reflection, the self-inflicted-drop exemption via a synthetic move, the Intimidate-specific redirect, and a dedicated Guard-Dog-vs-Mirror-Armor doubles independence test), Neutralizing Gas suppression for all 4, and a negative control (51 tests)

**Note:** if you add a new file with `class_name`, run an import pass before the test scenes
will see it:

```bash
/home/rob/Godot_v4.3-stable_linux.x86_64 --headless --path . --import
```

### Data pipeline scripts

Moves, abilities, and (as of the M18 item-data-infrastructure session) items each
follow the same two-track pattern: a hand-authored Python dict as the
human-editable source of truth, regenerated into Godot-native `.tres` Resource
files the engine loads at runtime. Run any of these from the project root, then
re-import (see the note above) before running a scene that depends on new/changed
`.tres` files:

```bash
python3 scripts/gen_moves.py      # data/moves/move_NNNN.tres    (MoveData)
python3 scripts/gen_abilities.py  # data/abilities/ability_NNNN.tres (AbilityData)
python3 scripts/gen_items.py      # data/items/item_NNNN.tres    (ItemData)
```

All three scripts share one structure: a list of dicts (`MOVES`/`ABILITIES`/`ITEMS`),
a `DEFAULTS` dict (skip a field in the emitted `.tres` when it equals its class
default, keeping files lean), a `FIELD_ORDER` list (explicit emit order), a
`render()` function, and a `main()` that writes one file per entry to a fixed
`data/<kind>/` directory, ID zero-padded to 4 digits.

Loading convention: `MoveRegistry.get_move(id)` / `ItemRegistry.get_item(id)` are
path-convention loaders (`res://data/<kind>/<kind>_%04d.tres % id`, no preload
table — see `move_registry.gd`'s own doc comment for the full rationale). There is
no `AbilityRegistry` — abilities are loaded ad hoc per call site (`load("res://data/abilities/ability_%04d.tres" % id) as AbilityData`)
because `AbilityManager`'s actual mechanic dispatch is 100% hardcoded ability-ID
constants and never reads `.tres` fields at battle runtime; the `.tres` layer there
is a metadata catalog (name/description/ai_rating/interaction flags), not a
mechanism input. **Items follow the `MoveRegistry` pattern** (`ItemRegistry`,
`scripts/battle/core/item_registry.gd`), since item mechanics are likewise
hardcoded in `ItemManager` (`HOLD_EFFECT_*` constants) but item data may need
by-ID lookup from future non-battle code (team-building, UI) the same way moves
do.

**Item scope note:** as of M18a, `gen_items.py`'s `ITEMS` dict only contains the
40 items M18a actually implemented — every future M18 sub-tier (M18b onward) must
add its items there and regenerate `.tres` files, matching the moves/abilities
convention, rather than wiring item data inline into `ItemManager` or a test file.
See `docs/decisions.md`'s item-data-infrastructure entry for the full rationale
and the M18a-era gap this closed.
