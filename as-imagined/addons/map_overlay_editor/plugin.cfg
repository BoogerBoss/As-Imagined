[plugin]

name="Map Overlay Editor"
description="Click/drag editing of per-cell collision and elevation on a MapOverlay node. Editor-only by construction: this plugin does not exist at runtime, so the overlay's write half is unreachable in a running game."
author="As Imagined"
version="1.0"
script="plugin.gd"
